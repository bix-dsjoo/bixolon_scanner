# BIXOLON Scanner 디자인 시스템

이 문서는 Windows 현장 검수자가 촬영, 상품 확인, 최종 확정을 빠르고 안전하게 수행하기 위한 UI 계약입니다. 구현의 기준 소스는 `lib/theme/app_tokens.dart`, `lib/theme/app_theme.dart`이며 화면별 임의 색상이나 크기를 추가하지 않습니다.

## 디자인 방향

- **한 화면, 한 핵심 행동:** 현재 단계에서 가장 중요한 행동만 Primary로 표시합니다.
- **검수 장비다운 정밀함:** 표면과 경계는 중립적으로 유지하고 선택, 포커스, 핵심 행동에만 BIXOLON Orange를 사용합니다.
- **의미 있는 브랜드 표현:** BIXOLON 로고의 `X`에서 파생한 대각선은 프리뷰의 촬영 가이드에만 사용합니다. 헤더나 패널에 장식용 대각선을 반복하지 않습니다.
- **색상에 의존하지 않는 상태:** 모든 상태는 색상과 함께 아이콘 및 한국어 문구로 표시합니다.
- **오류와 재촬영의 분리:** 시스템 장애인 `ERROR`와 모델 판정인 `RECAPTURE`를 같은 시각 또는 행동으로 표현하지 않습니다.

## 토스플레이스 UI/UX 분석 반영

토스플레이스의 색상이나 화면을 복제하지 않습니다. 공식 제품과 SDK가 반복해서 사용하는 정보 구조와 상호작용 원칙을 BIXOLON 운영 맥락에 맞게 번역합니다.

| 공식 제품·템플릿 | 관찰한 패턴 | Scanner 적용 |
| --- | --- | --- |
| [토스 POS](https://tossplace.com/product/pos) | 주문·결제·재고·매출처럼 밀도 높은 업무를 한 작업 화면 안에서 전환 | 프리뷰와 검수 패널을 동시에 유지하고 현재 단계의 행동만 강조 |
| [Front 단일·다중 선택 Template](https://docs.tossplace.com/reference/plugin-sdk/front/template.html#단일선택-템플릿-renderselectpage) | 큰 Soft Gray 선택 카드, 체크 표식, 선택 결과에 따라 문구가 변하는 하단 고정 CTA | 선택은 왼쪽 띠가 아니라 **전체 면 + 전체 외곽선 + radio/check**로 표시하고, 검수 진행도에 따라 같은 위치의 CTA 문구·활성 상태를 변경 |
| [Front 검색 Template](https://docs.tossplace.com/reference/plugin-sdk/front/template.html#검색화면-템플릿-rendersearchpage) | 상단 지시문 → 검색 입력 → 전체 선택 행의 단순한 순서, 선택 불가 결과는 색과 탭 행동을 함께 제거 | 상품 검색을 선택 객체 안에서 단계적으로 열고, 저장 중 선택 행은 `disabledSurface`·`disabledContentOpacity`·disabled Semantics로 잠금 |
| [Front Toast·결과 Template](https://docs.tossplace.com/reference/plugin-sdk/front/template.html#토스트-메시지-opentoast) | 성공은 상단 toast 또는 중앙 결과 상태로 분리하고 다음 행동은 하단 고정 영역에 한정 | 연속 검수 성공은 전역 상단 toast로 작업 맥락을 유지하고, 오류·재촬영은 중앙 상태 설명과 단일 복구 CTA로 분리 |
| [토스 키오스크](https://tossplace.com/product/kiosk) | 휴대폰처럼 익숙한 큰 선택 영역, 매장별 첫 화면과 메뉴 배열 | 최소 44px 조작 영역, 48px Primary, 작업에 맞는 한국어 문구 |
| [예약관리](https://tossplace.com/product/booking) | 달력·목록·담당자처럼 목적에 따른 뷰 전환과 예약부터 결제까지 이어지는 흐름 | 스캔/활동 모드를 명확히 나누되 세션 상태는 유지 |
| [고객관리](https://tossplace.com/product/crm) | 필요한 고객 맥락은 결제 순간에 보여주고 설정·분석은 별도 깊이로 분리 | 상품 결과를 먼저 보여주고 Scan ID·모델 버전은 접힌 진단 정보로 이동 |
| [프랜차이즈](https://tossplace.com/product/franchise) | 대시보드에서 전체 현황을 먼저 파악하고 매장·상품 단위로 좁힘 | Activity에서 결과 건수와 상품을 먼저, 검색·입력원·기간으로 점진적으로 좁힘 |

이 프로젝트의 고유한 시각 요소는 BIXOLON `X`에서 파생한 프리뷰 촬영 가이드 하나로 제한합니다. 나머지는 토스플레이스처럼 조용한 표면, 명확한 선택, 직접적인 문구로 구성합니다.

## 토큰

색상 토큰은 세 계층으로 관리합니다.

1. **원시 토큰 `AppPalette`:** 브랜드·중립·상태색의 고정값
2. **의미 토큰 `AppSemanticColors`:** `brand`, `surface`, `success`, `error`처럼 화면 맥락과 무관한 역할
3. **컴포넌트 토큰 `AppComponentColors`:** `selectionSurface`, `selectionOutline`, `actionBarSurface`, `toastSurface`, `focusRing`, `previewScrim`, `previewGuideOutline`, `previewGuideAccent`처럼 상호작용·표면 역할

화면 코드는 가능한 한 의미·컴포넌트 토큰을 사용합니다. 원시 토큰을 직접 사용해 새로운 선택색이나 피드백색을 만들지 않습니다. 타이포그래피는 `AppTypography`, 간격은 `AppSpacing`, 높이·반경·모션은 `AppDesignTokens`, 핵심 행동 문구는 `AppActionCopy`, 프리뷰 출처 문구는 `AppPreviewCopy`를 단일 구현 기준으로 사용합니다.

### BIXOLON 원본 가이드 대응

- 브랜드 원본값은 `BIXOLON_Brand_Guidelines_English_Apr26_V1.pdf`의 Orange `#EE7203`, Orange Tint `#F28E00·#F7AA4E`, 승인 중립색 `#000000·#3F3D3E·#878786·#AEAEAE·#F5F5F5`를 기준으로 확인합니다.
- UI 본문 Ink `#171717`과 Selection Surface `#FFF4E9`는 앱 접근성과 선택 피드백을 위한 파생 토큰이며 공식 로고나 브랜드 텍스트에 적용하지 않습니다.
- 공식 로고는 앱에서 재제작하지 않으며, `X` 기반 대각선은 원본 가이드의 Visual Concept을 스캔 가이드로 제한해 번역합니다.

### 색상

| 역할 | 값 | 사용 |
| --- | --- | --- |
| Brand | `#EE7203` | Primary CTA, 선택선, 촬영 가이드 |
| On Brand | `#171717` | Brand 배경 위 텍스트와 아이콘 |
| Focus Ring | `#D96500` | 밝은 Surface에서 3:1 이상을 유지하는 키보드 포커스 경계 |
| Selection Surface | `#FFF4E9` | 선택 행과 선택 옵션에만 사용하는 파생 배경 |
| Ink | `#171717` | 제목, 본문, 핵심 정보 |
| Muted | `#696767` | 보조 설명과 메타데이터 |
| Workspace | `#F5F5F5` | 앱 작업 영역 배경 |
| Surface | `#FFFFFF` | 패널과 컨트롤 표면 |
| Outline | `#E3E3E1` | 패널, 행, 입력 경계 |
| Disabled Content | `#696767` | `#E8E8E6` 비활성 면 위의 읽을 수 있는 이유·상태 문구 |
| Success | `#16865A` | 완료, 승인, 저장됨 |
| Attention | `#B45F06` | 확인 필요, 재촬영 |
| Error | `#B42318` | 분석 서버, 저장, 입력 오류 |
| Preview Guide Outline | `16% White` | 어두운 프리뷰 위 촬영 범위와 중앙 표식 |
| Preview Guide Accent | `90% Brand` | BIXOLON 대각선 코너 가이드 |

Brand 배경에는 흰색 텍스트를 사용하지 않습니다. `#EE7203`과 흰색의 대비는 일반 본문 기준을 충족하지 않으며 Ink와의 대비는 4.5:1 이상입니다. Brand Orange는 흰색에서 2.98:1이므로 단독 포커스 경계에는 사용하지 않고, 포커스 역할은 파생 Deep Orange `#D96500`으로 분리해 Surface·Workspace·Brand Soft에서 3:1 이상을 유지합니다.

### 타이포그래피

Pretendard Variable을 기본으로 사용하고 미지원 환경에서는 `Segoe UI`, `Malgun Gothic`, `Arial` 순으로 대체합니다.
글꼴·크기·행간·굵기의 구현 기준은 `AppTypography`이며, 화면이 독자적인 `TextStyle`로 글꼴 상속을 끊지 않습니다.

| 역할 | 크기/행간 | 굵기 |
| --- | --- | --- |
| 화면 제목 | `24/32` | 700 |
| 섹션 제목 | `18/26` | 700 |
| 본문 | `16/24` | 400 |
| 보조 본문 | `14/20` | 400~700 |
| 캡션 | `12/18` | 400~700 |
| 컴팩트 필터·칩 | `13/20` | 600~700 |
| CTA | `15/20` | 700 |

굵기는 `regular 400`, `medium 500`, `semibold 600`, `bold 700`만 사용합니다. 화면 제목·섹션 제목·앱명·데이터 레이블 tracking도 `AppTypography`에 정의하며 화면 코드에서 `FontWeight`, 글자 크기, 자간 숫자를 직접 만들지 않습니다.

### 크기와 간격

- 간격은 `4, 8, 12, 16, 24, 32px`만 사용합니다.
- 공통 컴포넌트와 화면의 패딩·요소 간 간격은 숫자를 직접 복제하지 않고 `AppSpacing.x1/x2/x3/x4/x6/x8`에 연결합니다. 아이콘의 광학 크기와 모델 좌표는 간격 토큰 대상에서 제외합니다.
- 최소 조작 영역은 `44px`, 주요 행동은 `48px`, 목록 행은 `60px`, 상단·패널 헤더는 `60px`, 섹션·표 헤더는 `40px`, 고정 액션 바는 최소 `72px`입니다.
- 운영 레이아웃 수치는 `AppDesignTokens`와 `AppBreakpoints`에서 관리합니다. 스캔 결과 패널은 폭 `36%`, 최소 `440px`, 최대 `520px`, 선택 객체 검수 영역은 최대 높이이자 다수 객체에서 우선 확보할 높이 `340px`, Activity 검색창은 `320px`이며 스캔 적층 전환점은 `960px`입니다. 확인 대화상자 `dialogWidth 400px`, 빈 상태 `emptyStateMaxWidth 360px`, 진단 레이블 `metadataLabelWidth 88px`도 화면에서 숫자를 복제하지 않습니다.
- 스캔 가이드는 프리뷰의 `64%` 범위, 대각선 코너 `34px`, 중앙 표식 `7px`, Brand 선 `2.5px`를 `AppDesignTokens`에서 관리합니다. `34px`·`7px`는 4px 간격을 만드는 레이아웃 값이 아니라 BIXOLON 시그니처의 광학 치수이므로 리듬 예외이며, 화면이나 painter에서 원시값을 다시 만들지 않습니다.
- 비조작 상태 요소는 `compactVisualSize 32px`, 단계 탐색 중앙 레이블은 `stepNavigatorLabelWidth 52px`, 프리뷰 출처 칩은 최소 `previewLabelMinHeight 36px`와 `previewSourceMarkerSize 6px`, 상단 탐색의 현재 위치 표시는 `navigationIndicatorThickness 3px`를 사용합니다. 진행 표시는 문맥에 따라 `inline 20px`, `preview 24px`, `page 28px`로 구분하며 모두 `AppDesignTokens`에서 관리합니다. 콘텐츠가 커지면 상태 배지와 출처 칩은 고정 높이에 가두지 않고 최소 높이 위로 확장합니다.
- Windows의 적응형 compact 밀도로 크기가 줄지 않도록 `VisualDensity.standard`와 padded tap target을 명시합니다.
- 컨트롤 반경은 `8px`, 패널과 다이얼로그는 `12px`, 상태 배지는 `pillRadius`로 완전한 pill 형태를 유지합니다. 키보드 포커스는 `focusRingWidth 2px`, 선택 표면은 `selectionOutlineWidth 1.5px`를 Theme·필터·드롭다운·공통 선택 표면이 공유합니다. 선택 불가 콘텐츠는 `disabledContentOpacity 0.56`을 사용하고 배경·Semantics·pointer·keyboard 행동도 함께 비활성화합니다.
- 짧은 상태 전환은 `160ms`, 패널 전환은 `240ms`, 완료 피드백 유지 시간은 `3초`를 사용합니다. 상호작용 전환 곡선은 `AppMotion.interactionCurve`의 `easeOutCubic` 하나로 통일합니다. 전환 애니메이션은 시스템의 reduced motion 설정을 존중해 즉시 완료하고, 자동 스크롤은 점프하며, 불확정 진행 표시는 회전 대신 정적인 대기 아이콘으로 바뀝니다.

## 공통 컴포넌트

- **Brand mark:** 공식 로고를 재제작하지 않습니다. Orange 포커스 아이콘과 `BIXOLON Scanner` 앱명을 사용합니다. 같은 포커스 심볼을 Windows 제목 표시줄·작업 표시줄·실행 파일 아이콘에도 적용해 Flutter 기본 로고가 제품 표면에 남지 않도록 합니다. 네이티브 ICO는 `tool/generate_windows_icon.py`로 재생성하며 Brand Orange·Ink 역할을 그대로 사용합니다.
- **Navigation item:** 아이콘과 한국어 레이블, Orange 하단선으로 현재 작업 영역을 표시합니다.
- **Status badge:** 아이콘과 경계에는 상태색, 문구에는 Ink를 사용합니다. 장식이나 카테고리 분류에는 사용하지 않습니다.
- **Status priority:** 카메라 미연결은 스캔 화면에서 카메라 촬영을 막을 때만 Attention으로 표시합니다. 이미지 검수 중에는 `이미지 입력 · 카메라 미연결`, Activity에서는 `카메라 미연결`의 중립 배지로 낮추어 현재 작업과 경고가 경쟁하지 않게 합니다.
- **Panel header:** `headerHeight`의 최소 `60px` 안에 제목, 선택적 설명, 한정된 보조 행동만 포함합니다.
- **Section header:** `sectionHeaderHeight`의 최소 `40px`를 사용하며 목록 수량이나 표 컬럼처럼 다음 콘텐츠의 구조를 설명하는 데만 사용합니다.
- **Empty state:** 상황, 다음 행동 순서로 설명합니다. 모호한 사과 표현을 사용하지 않습니다.
- **Filter group:** 레이블과 동일한 선택 칩 간격·선택 면을 제공하며 입력원·기간처럼 상호 배타적인 필터에 사용합니다.
- **Search field:** 검색어가 없으면 `/` 단축키를, 입력 중이면 44px 이상의 지우기 행동을 같은 suffix 위치에 표시합니다. 검색·필터·정렬 중 하나라도 기본값이 아니면 인접한 `모두 초기화`로 전체 조건을 되돌릴 수 있어야 합니다.
- **Keyboard shortcut hint:** 단축키가 연결된 입력에는 일반 키보드 아이콘 대신 실제 키 문자를 24px 키캡으로 표시합니다. 키캡은 조작 요소나 별도 Tab 정지점이 아니며 Tooltip·Semantics로 의미를 전달합니다.
- **Icon action:** 새로고침·이전/다음·검색어 지우기·검색 닫기 같은 아이콘 단독 행동은 모두 48px 고정 조작면과 Tooltip을 사용하고 평상시 별도 표면을 추가하지 않습니다. 키보드 포커스에서만 사방 2px Focus Ring을 표시하며, 진행 중에는 같은 위치의 스피너·구체적인 live region·disabled 상태로 전환합니다.
- **Button focus:** Filled Primary는 Orange 면과 4.5:1 이상 대비되는 Ink 2px 경계를, Outlined·Text 행동은 Focus Ring 2px 경계를 사용합니다. 포커스는 면색을 추가하지 않아 선택·hover·pressed 상태와 혼동하지 않으며 Theme의 상태 토큰으로 모든 화면에 동일하게 적용합니다.
- **Preview source label:** 프리뷰 좌상단 칩은 현재 단계를 반복하지 않고 `라이브 카메라`, `촬영 이미지`, `선택한 이미지`, `카메라 미리보기` 중 실제 입력 출처만 표시합니다. `AppPreviewCopy`를 시각 문구와 `입력 미리보기, …` Semantics의 단일 소스로 사용합니다.
- **Scan guide:** `AppScanGuide`는 실제 카메라 프리뷰 또는 입력 이미지가 있고 정렬·분석·재촬영에 도움이 되는 Scan 상태에서만 촬영 범위와 BIXOLON 대각선 코너를 표시합니다. 비율·광학 치수·색 역할은 ThemeExtension을 소비하고 장식이므로 포인터와 Semantics에서 제외합니다. 카메라 확인 중·미연결·복구 필요, `ERROR`, 결과 bounding box가 나타난 상태에서는 제거하며 Activity·선택 행·CTA에는 사용하지 않습니다. 모델이 촬영 구도를 고치라고 판정한 `RECAPTURE`에는 유지해 시스템 장애와 시각적으로도 분리합니다.
- **Selectable surface:** 선택되면 전체 면에 Brand Soft와 사방 1.5px Brand 외곽선을 표시합니다. 확정 후보처럼 값이 선택된 경우에는 radio/check, 객체·Activity처럼 현재 상세 대상을 가리키는 경우에는 화살표를 사용합니다. 왼쪽 띠는 사용하지 않습니다. pointer `tapDown`과 키보드 `ActivateIntent`를 구분해 동적 단계 전환의 포커스 인계는 Enter·Space 사용 시에만 실행합니다.
- **Candidate row:** Top-3와 전체 상품 검색 결과를 같은 60px 선택 행으로 표시합니다. 상품명을 우선하고 class ID와 confidence는 오른쪽 보조 정보로 낮춥니다. 현재 항목 번호와 선택 후 다음 항목으로 이동한다는 결과를 검수 영역에서 먼저 안내합니다. 검색 결과도 전체 면 hover·사방 포커스 링·단일 Semantics를 공유합니다. 레이블은 상품명·신뢰도만 읽고 버튼 역할과 tap 액션이 행동을 담당하며, Top-3·검색 결과는 보조기기에 상호 배타적인 단일 선택 그룹으로 노출합니다.
- **Step navigator:** 선택된 다중 객체가 있을 때만 `이전 | 현재/전체 | 다음`을 하나의 컨트롤로 묶어 표시합니다. 외곽선이 내부 조작 영역을 줄이지 않게 이전·다음 각각 정확히 48px를 유지하고 독립적인 포커스 링을 제공합니다. 항목이 하나이거나 선택이 해제된 완료 상태에서는 숨겨 최종 확정 행동과 경쟁하지 않게 합니다.
- **Detection box:** 모델 좌표의 시각 경계는 그대로 유지하되 클릭·Semantics 영역은 최소 44px로 확장합니다. 현재 검수 box는 항상 마지막에 그려 Brand 경계·저농도 면·`n 현재 검수` 레이블로 표시하고, 비선택 확정 box는 번호, 확인 필요 box는 `n ?`로 밀도를 낮춥니다. 전체 상태 문구는 결과 목록과 Semantics에서 제공합니다.
- **Activity row:** 확정 시각·상품 요약·입력원·`자동 확정/n개 수정`을 표시합니다. 처리시간처럼 현재 기록을 선택하는 데 필요하지 않은 진단 값은 목록에 노출하지 않습니다.
- **Diagnostic disclosure:** Activity 상세의 기본 깊이에는 확정 상품명과 수정 여부만 표시합니다. Scan ID·처리시간·모델 버전과 객체별 item ID·confidence·확정 방식은 하나의 접힌 `진단 정보`에서 제공합니다. 헤더는 60px이며 접힘·펼침 상태, chevron, 사방 포커스 링과 Enter·Space 실행을 함께 제공합니다.
- **Confirmation dialog:** 진행 중인 상품 확인 내용을 버리는 이미지 교체·재촬영은 400px 대화상자에서 질문과 구체적인 손실 결과를 먼저 설명합니다. `취소 | 실행 동사`를 같은 48px 폭의 행동으로 제공하고 안전한 `취소`에 초기 포커스를 둡니다. Tab으로 실행 행동에 이동하며 Enter로 현재 행동을 실행하고 Esc는 변경 없이 닫습니다. 중립 행동은 Focus Ring, Orange Primary는 대비가 보이는 Ink 포커스 링을 사용합니다.
- **Action bar:** 최소 72px의 고정된 표면에 화면 상태당 Primary action을 최대 하나만 배치합니다. 다중 검수에서는 같은 위치의 CTA를 유지하고 `남은 상품 확인 필요 → n개 상품 최종 확정 → 저장 중`으로 문구와 활성 상태만 전환합니다. 저장 실패 시 확인 결과와 FocusNode를 유지한 채 같은 위치를 `다시 저장`으로 전환해 키보드 Enter 재시도까지 완료 경로를 끊지 않습니다. 성공하면 실패 CTA를 제거하고 단일 완료 live region으로 확정을 알리며 다음 Tab 탐색을 새 작업 흐름에서 시작합니다. 진행 중 사용할 수 없는 Secondary는 회색 disabled 버튼으로 남기지 않고 숨기며, 실제로 계속 사용할 수 있는 카메라 연결 확인 중 `이미지 선택`만 유지합니다.
- **Progress action:** 시간이 걸리는 Primary action은 같은 버튼 위치·크기를 유지하고 진행 아이콘과 현재 동사로 전환합니다. 진행 중 버튼은 재실행할 수 없으며, 시각 문구보다 구체적인 단일 live region으로 `현재 작업 + 기다려야 하는 이유`를 알립니다.
- **Progress visual:** inline `20px`, preview `24px`, page `28px`의 같은 크기 슬롯을 유지합니다. 일반 환경에서는 회전형 진행 표시를 사용하고 reduced motion에서는 정적인 대기 아이콘을 사용합니다. 진행 시각 자체는 Semantics에서 제외하고 인접 문구 또는 단일 live region이 현재 작업을 설명합니다.
- **Toast:** 결과를 만든 행동의 완료 여부를 상단의 동일한 위치·형태·문구 체계로 3초 동안 알립니다. 스캔·Activity 전환과 무관한 전역 레이어에 표시하고 pointer 입력을 가로막지 않으며 단일 live region을 제공합니다. 연속 완료 시 이전 만료 타이머를 취소해 새 메시지의 전체 수명을 보장합니다.
- **Inline notice:** 기존 콘텐츠를 계속 사용할 수 있는 비차단 오류는 상태색의 저농도 면, 아이콘, 원인·현재 상태 문구, 선택적 보조 행동으로 콘텐츠 위에 표시합니다. 전체 오류 화면이나 Primary action으로 격상하지 않습니다. 같은 복구 행동이 툴바에도 있으면 오류가 유지되는 동안 툴바 행동을 숨겨 문맥 CTA 하나만 남깁니다. 키보드로 툴바 행동을 실행했다면 실패 시 인라인 복구 행동, 재시도 성공 시 원래 툴바 행동으로 포커스를 인계합니다. 진행 중 사용자가 다른 컨트롤로 이동하면 완료 후 포커스를 되가져오지 않습니다.
- **Loading state:** 아직 사용할 데이터가 없는 최초 로드는 작동하지 않는 도구를 숨기고 진행 표시와 `무엇을 불러오는 중인지` 설명하는 상태 레이블을 표시합니다. 기본은 단일 live region이며, 직전에 더 중요한 완료 알림이 있는 자동 동기화만 `announce: false`로 시각·접근 가능한 레이블을 유지하고 자동 발화를 끕니다. 기존 데이터가 있는 새로고침은 이 상태로 전환하지 않습니다.
- **State announcement:** 시각 화면을 추가하지 않고 완료 상태의 의미만 즉시 전달해야 할 때 `AppStateAnnouncement`를 사용합니다. 검수 중에는 현재 상품 제목 live region 하나를 유지하고, `APPROVED` 또는 마지막 상품 확인으로 모든 검수가 끝난 순간에만 `n개 상품 확인 완료. 최종 확정할 수 있어요.`를 알립니다. 화면 공간과 포커스·tap 행동을 만들지 않습니다.
- **State message:** `ERROR`·`RECAPTURE`·카메라 문제·Activity 빈/오류처럼 작업을 멈추거나 방향을 바꾸는 상태는 제목과 해결 설명을 하나의 live region으로 알립니다. 아이콘·제목·설명을 중복 발화하지 않으며 인접한 Primary 복구 행동은 별도 버튼 노드로 유지합니다. 검색·필터 결과가 없을 때는 편집 도구를 유지하되 상단의 중복 초기화를 숨기고 중앙 `모두 초기화` 하나를 Primary로 제공합니다.
- **Data row:** 60px 높이, 1px 구분선, 전체 선택 면, 보조 메타데이터 순으로 구성합니다.

### 선택 표면 상태

| 상태 | 면 | 경계·표식 |
| --- | --- | --- |
| Resting | Surface | 기본 구분선과 chevron |
| Hover | `rowHover` | 기존 경계를 유지하고 전체 면만 중립 hover로 전환 |
| Selected | `selectionSurface` | 사방 1.5px `selectionOutline`과 맥락별 radio/check 또는 화살표 |
| Selected filter | `selectionSurface` | 폭을 바꾸는 왼쪽 띠·체크 추가 없이 사방 1.5px `selectionOutline`과 Bold 레이블 |
| Focused | 현재 면 유지 | 선택 여부와 무관한 사방 2px `focusRing` |
| Disabled Primary | `disabledSurface` | `disabledContent`, 실행 불가 상태와 원인을 인접 문구로 표시 |

상태 우선순위는 `Focused border > Selected border > resting border`, 면 우선순위는 `Selected > Hover > resting`입니다. Hover는 마우스 사용자의 예측 피드백이며 동일한 기능은 키보드 포커스와 Enter·Space로도 사용할 수 있어야 합니다.

`AppSelectableSurface.inMutuallyExclusiveGroup`은 한 번에 하나의 현재 대상만 가질 수 있는 객체 목록·Top-3/검색 결과·Activity 목록에서 활성화합니다. 선택 상태는 `Semantics.selected`, 관계는 `inMutuallyExclusiveGroup`, 실행은 button·tap으로 분리하며 레이블에 `선택`을 반복하지 않습니다.

### 마이크로카피

- 사용자가 누른 행동과 확인 대화상자의 질문·확정 버튼은 같은 대상과 동사를 사용합니다. `AppActionCopy`의 `촬영하기`, `다시 촬영`, `분석하기`, `분석 중`, `다시 분석`, `다시 연결`, `연결 확인 중`, `새로고침`, `모두 초기화`, `저장 중`, `다시 저장`을 화면별로 변형하지 않습니다.
- 이미지 입력은 `다른 이미지 선택 → 다른 이미지를 선택할까요? → 다른 이미지 선택`, 카메라 입력은 `다시 촬영 → 다시 촬영할까요? → 다시 촬영`의 흐름을 유지합니다.
- 사용자 화면에서는 구현 용어인 `Worker` 대신 `분석 서버`를 사용합니다. `Scan ID`, 모델명, reason code, 상태 enum처럼 식별에 필요한 기술 용어만 영어로 유지합니다.
- 앱과 Windows 창의 제품명은 `BIXOLON Scanner`로 통일합니다.
- 질문은 결과를 먼저 알 수 있는 짧은 능동형 문장으로 쓰고, 되돌릴 수 없는 영향은 본문 한 문장으로 분리합니다.

## 화면 계약

### 스캔

- 지원 기준은 `1280×720` 이상이며 `1440×900`을 주요 검증 크기로 사용합니다.
- 결과 패널은 `AppDesignTokens`의 폭 비율 `36%`를 사용하되 `440~520px` 범위로 제한합니다. `1280px`에서는 `460.8px`, `1440px`에서는 `518.4px`로 실제 렌더되는지 위젯 테스트로 고정합니다.
- 프리뷰, 객체 목록, 선택 객체 검수, 고정 확정 푸터 순서로 정보를 제공합니다.
- 객체 목록은 실제 행 수만큼만 높이를 사용하고 선택된 행 바로 다음에 Top-3 또는 상품 검색을 이어 붙입니다. 객체가 많으면 검수 영역 `340px`를 먼저 확보하고 목록만 스크롤해 현재 행·선택지·대체 검색 행동을 한 화면에 유지합니다.
- Bounding box와 객체 목록 선택은 항상 동기화합니다.
- 프리뷰 번호는 `item_id`의 문자열 형식이 아니라 응답 배열의 표시 순서에서 계산해 결과 목록의 `01·02…`와 항상 일치시킵니다. 밀집·중첩 상황에서도 선택 box를 최상단에 그립니다.
- 프리뷰 출처 칩은 작업 단계나 결과를 반복하지 않고 사용자가 지금 보고 있는 입력이 라이브 카메라인지, 촬영 이미지인지, 선택한 이미지인지만 식별합니다. 단계와 검수 진행은 결과 패널 헤더·목록에서 제공합니다.
- 후보 확정, 목록 클릭, 방향키 이동으로 선택 객체가 바뀌면 결과 목록을 자동 스크롤해 현재 행을 항상 보이는 영역에 유지합니다. 이 이동은 판정이나 확정 상태를 바꾸지 않습니다.
- 선택된 객체가 둘 이상일 때 결과 헤더의 단계 탐색기는 현재 위치를 `n / 전체`로 표시합니다. 모든 객체가 확정되어 선택이 해제되거나 처음부터 단일 객체이면 탐색기를 표시하지 않습니다.
- 확정 푸터는 검수 중에도 사라지지 않으며 남은 수량을 표시한 비활성 CTA로 완료 조건을 예고합니다. 모든 상품이 확인된 뒤에만 동일한 CTA가 활성화되며 기존 제출 조건은 바꾸지 않습니다.
- `RECAPTURE`와 `ERROR`에서는 item 목록과 최종 확정 행동을 표시하지 않습니다.

#### 준비 단계

| 상황 | 패널 단계·안내 | Primary |
| --- | --- | --- |
| 카메라 초기화·재연결 진행 | `카메라 확인 중` · 연결 상태 확인 | 비활성 `연결 확인 중`; 이미지 선택은 계속 가능 |
| 카메라 미연결 | `입력 준비` · 카메라 연결 또는 이미지 파일 선택 | `다시 연결` |
| 카메라 연결 완료 | `촬영 준비` · 가이드 안에 상품 배치 | `촬영하기`; `이미지 선택`은 Secondary |
| 이미지 선택 완료 | `분석 준비` · 이미지에서 상품을 찾을 준비 완료 | `분석하기` |
| 분석 실행 | `분석 중` · 현재 이미지 처리 | 진행 `분석 중` 하나만 표시; 새 입력과 최종 확정은 숨김 |

화면 언어는 `프리뷰 칩=입력 출처`, `패널 헤더=현재 단계`, `본문=상황과 안내`, `Action bar=다음 행동`으로 역할을 분리합니다. 분석 중 프리뷰는 문구를 반복하지 않고 진행 표시만 제공하며, 구체적인 대기 안내는 진행 CTA의 단일 live region이 담당합니다. `아직 분석된 상품이 없어요`처럼 결과 부재만 설명하지 않고 사용자가 지금 할 수 있는 행동을 먼저 안내합니다.

상품명은 운영 카탈로그의 한국어 `display_name_ko`를 화면·Semantics·신규 Activity 로그에 사용합니다. 모델 계약의 영문 `class_name`은 API·진단·검색 호환성을 위해 보존하고 `class_id`와 함께 검색할 수 있지만, 사용자에게 보이는 기본 상품명으로 사용하지 않습니다. `display_name_ko`가 없는 카탈로그 항목은 조용히 영문으로 대체하지 않고 시작 시 형식 오류로 거부합니다.

### 오류 복구

| 상태·원인 | 시각·문구 | Primary 복구 행동 |
| --- | --- | --- |
| `ERROR` · 손상/미지원/과대 이미지 | Error 색상, 손상 이미지 아이콘, 구체적인 입력 조건 | 이미지 입력은 `다른 이미지 선택`, 카메라 입력은 `다시 촬영` |
| `ERROR` · 연결/시간 초과/서버 응답 | Error 색상, 연결 끊김 아이콘, 서버 또는 처리 원인 | 현재 이미지를 유지한 `다시 분석` |
| `RECAPTURE` · 모델 촬영 부적합 판정 | Attention 색상, 촬영 가이드 아이콘, 구도·조명 등 교정 방법 | 입력 방식에 맞는 `다른 이미지 선택` 또는 `다시 촬영` |
| Flutter 카메라 초기화·재연결 실패 | Attention 색상, `카메라 확인 필요`, `카메라를 사용할 수 없어요`와 연결 확인 안내 | `다시 연결` |
| Flutter 카메라 촬영 호출 실패 | Attention 색상, `카메라 확인 필요`, `촬영하지 못했어요`와 응답·복구 안내 | `다시 연결` |
| Flutter 로컬 저장 실패 | Error 색상, `저장하지 못했어요. 확인 결과는 유지됐어요.` 단일 live region | 보존된 동일 결과의 `다시 저장` |

공개 상태는 계속 `ERROR` 또는 `RECAPTURE`이며 Flutter 내부의 `ScannerErrorRecovery`만 다음 행동을 결정합니다. 입력 오류를 `RECAPTURE`로 바꾸거나 연결 오류에 재촬영을 요구하지 않습니다. 로컬 저장 실패도 Worker 상태를 바꾸지 않고 `reviewing`과 확정 결과·원본 입력을 유지합니다. 버튼과 복구 본문은 `다시 시도`처럼 모호한 표현 대신 실제 실행인 `다시 분석`, `새로고침`, `다른 이미지 선택`, `다시 촬영`, `다시 저장`을 같은 동사로 사용합니다.

카메라 초기화·재연결·촬영 호출 실패는 Worker 응답이 아니므로 공개 `ERROR` 또는 `RECAPTURE`로 변환하지 않습니다. 내부 `CameraIssueType`은 장치 사용 불가 `unavailable`과 촬영 호출 실패 `captureFailed`를 구분해 사용자가 실제로 수행한 단계와 제목을 일치시킵니다. 최초 촬영 실패는 `cameraMessage`만 설정하고 `processState: ready`·응답 `null`을 유지합니다. 기존 `RECAPTURE` 또는 `ERROR`에서 다시 촬영하다 실패한 경우에는 이전 응답을 내부에 보존하되, 상단 배지·프리뷰·헤더·본문·Action bar 모두 현재 카메라 복구 상태가 우선합니다. 활성 Primary는 `다시 연결` 하나로 교체합니다. 재연결 Future가 진행되는 동안에는 `isCameraCheckActive`가 이전 판정보다 우선하고 비활성 `연결 확인 중`으로 중복 실행을 막으며, 성공 후 메시지를 지워 이전 판정과 재촬영 행동으로 안전하게 돌아갑니다.

### 활동

- 최근 100건에 대해 상품명/Scan ID 검색, 입력원, 기간, 정렬 필터를 로컬로 적용합니다.
- 저장 기록이 없거나 최초 로드가 실패한 상태에서는 `활동 기록` 제목만 유지하고 작동할 수 없는 검색·필터·정렬을 숨깁니다. 빈 상태는 `스캔 화면으로 이동`, 최초 오류는 `새로고침` 하나만 Primary로 제공합니다.
- 필터 순서는 `검색 AND 입력원 AND 기간`이며 그 결과를 최신순 또는 오래된순으로 정렬합니다.
- 수량은 상단 제목에서 반복하지 않고 목록 헤더에만 표시합니다. 기본 상태는 `n건`, 검색·입력원·기간 조건이 있으면 `표시 n / 전체 n건`과 `검색 결과`로 맥락을 함께 제공합니다.
- 검색어는 입력 필드에서 바로 지울 수 있고 검색·필터·정렬은 `모두 초기화`로 한 번에 기본값으로 되돌립니다. 일부 결과가 남아 있어도 초기화 행동을 숨기지 않습니다.
- 여러 상품이 포함된 기록은 목록에서 `첫 상품 외 n개`로 요약합니다. 상품명 검색 중에는 실제로 검색어와 일치한 상품을 첫 항목으로 올려 결과가 나타난 이유를 보여주고, 상세에서는 전체 상품을 저장 순서대로 표시합니다.
- 검색·입력원·기간 조합의 결과가 0건이면 검색창과 필터는 직접 수정할 수 있도록 유지합니다. 전체 검색·필터·정렬을 지우는 중앙 `모두 초기화`만 Primary로 제공하고 같은 행동의 상단 Text button은 숨깁니다.
- 목록은 확정 시각·상품·입력원·검수 결과만 표시하고, 검수 결과는 `자동 확정` 또는 `n개 수정`으로 즉시 구분합니다.
- 상세의 기본 깊이에는 확정 상품명과 `수정됨`만 표시합니다. Scan ID, 처리시간, 모델 버전, 객체별 item ID·confidence·확정 방식은 접힌 `진단 정보`에 배치합니다.
- 스캔 화면으로 이동해도 검색, 필터, 선택 기록을 앱 세션 동안 유지합니다.
- 최초 로딩은 콘텐츠 영역의 진행 상태로 표시하지만, 기록이 이미 있으면 새로고침 중에도 목록·선택·상세를 그대로 유지하고 새로고침 버튼 위치에서만 진행을 알립니다. 진행 중 추가 클릭과 `F5`는 새 요청을 만들지 않습니다.
- 기존 기록이 있는 새로고침만 실패하면 `새로고침하지 못했어요. 기존 활동을 표시하고 있어요.` 인라인 알림과 `새로고침` 하나를 제공하고 기존 목록을 유지합니다. 오류가 유지되는 동안 같은 툴바 아이콘은 숨기며 `F5`는 계속 작동합니다. 재시도를 시작하면 알림을 닫고 원래 툴바 위치의 진행 상태로 전환합니다. 최초 로딩 실패만 전체 오류 상태로 전환합니다.
- 정렬·필터·새로고침으로 선택 기록의 목록 위치가 바뀌면 해당 행을 자동 스크롤해 보이는 영역에 유지합니다. 선택 상세와 세션 상태는 바꾸지 않습니다.
- 긴 목록의 자동 스크롤은 `60px` 행 경계에 맞춥니다. viewport 높이가 행 높이의 배수가 아니면 나머지만큼 하단 buffer를 계산해 컬럼 헤더 아래 첫 기록과 마지막 선택 기록을 모두 잘림 없이 표시합니다.
- 기록 로드 오류 시에는 작동할 수 없는 검색·필터·정렬을 숨기고 상태 설명과 `새로고침` 행동만 노출합니다. 기존 필터 값은 재시도 후에도 유지합니다.

## 키보드와 접근성

| 키 | 동작 |
| --- | --- |
| `Ctrl+O` | 이미지 선택. 분석·저장 중에는 비활성 |
| `↑` / `↓` | 이전/다음 검수 객체 |
| `/` | 상품 또는 활동 검색에 포커스 |
| `Esc` | 검색 또는 활성 오버레이 닫기 |
| `F5` | 활동 기록 새로고침 |

- 텍스트 입력 중에는 객체 이동과 `/` 단축키를 가로채지 않습니다.
- 단축키의 가용성은 화면 행동과 같아야 합니다. 분석·저장 중처럼 이미지 선택 행동을 숨긴 상태에서는 스캔과 Activity 어느 화면에서도 `Ctrl+O`가 입력 선택이나 변경 확인 대화상자를 실행하지 않으며, 처리된 키로 가장하지도 않습니다. 카메라 연결 확인 중에는 실제 이미지 입력이 가능하므로 화면 버튼과 `Ctrl+O`를 함께 유지합니다.
- 촬영, 분석, 최종 확정에는 단축키를 제공하지 않습니다.
- 일반 텍스트는 최소 4.5:1, 큰 텍스트와 UI 경계는 최소 3:1 대비를 유지합니다.
- `1280×720` 논리 작업대에서 125%·150% 큰 글자를 지원합니다. Windows 150% 표시 배율의 `1920×1080 physical → 1280×720 logical` 환경도 동일한 레이아웃 계약을 유지합니다.
- confidence·처리시간·수량처럼 한 단위로 읽어야 하는 수치는 tabular figures와 줄바꿈 금지를 사용하고, 확대 시에도 `%`·`ms` 단위를 분리하지 않습니다.
- 포커스는 Focus Ring `#D96500` 2px 경계 또는 동등한 가시적 표시로 확인할 수 있어야 합니다.
- 선택 표면의 키보드 포커스는 선택 여부와 관계없이 사방 2px Focus Ring으로 표시합니다. 선택의 Brand Orange보다 약간 깊은 색으로 현재 키보드 위치와 선택값을 구분합니다.
- 마우스가 선택 가능한 전체 면에 진입하면 `rowHover`로 면을 전환하고 클릭 커서를 표시합니다. 이미 선택된 면은 hover가 덮지 않습니다.
- 상단 탐색은 `스캔 → 활동` 순서로 Tab 이동하며 Enter·Space로 실행합니다. 선택 하단선과 키보드 포커스 사방선은 다른 레이어로 구분합니다.
- Activity의 Tab 순서는 `검색 → 검색어 지우기(입력 중) → 새로고침 → 입력원 필터 → 기간 필터 → 모두 초기화(활성 시) → 정렬 → 기록 목록`의 시각적 읽기 순서를 따릅니다. `AppFilterChip`은 포커스 시 사방 2px `focusRing`, 선택 시 1.5px `selectionOutline`을 사용하고 Space로 선택합니다. 정렬 드롭다운도 `AppDropdownControl`의 사방 2px `focusRing`으로 현재 위치를 표시합니다.
- Activity 필터는 화면의 그룹 제목에만 의존하지 않고 각 칩을 `입력원, 전체`, `기간, 전체`처럼 `그룹, 값`으로 읽습니다. 각 칩은 중복 없는 단일 Semantics 노드에서 `button`·`selected`·`focused`·상호 배타 그룹·`tap`을 함께 제공합니다.
- 검색이 비어 있을 때의 `/` 키캡은 `검색 단축키: /`로 읽히지만 Tab 정지점은 만들지 않습니다. Activity 도구는 명시적 순서 그룹을 사용해 검색 필드가 첫 Tab 위치를 유지합니다.
- 비차단 인라인 알림은 오류 문구를 독립 `liveRegion`으로 한 번만 알리고, 인접한 `새로고침` 같은 복구 행동은 별도 버튼·탭 노드로 유지합니다. 메시지 중복 제거를 위해 행동까지 `excludeSemantics`하지 않습니다.
- 전체 상태 메시지는 `제목. 해결 설명`을 하나의 `liveRegion`으로 제공하고 아이콘·시각 텍스트의 하위 Semantics만 합칩니다. `다시 분석`, `다른 이미지 선택`, `다시 연결`, `새로고침` 같은 복구 행동은 이 노드에 합치지 않아 독립적인 `tap`을 유지합니다.
- 선택 표면은 하나의 Semantics 노드로 노출하고 `button`·`selected`·`tap` 정보와 명확한 레이블을 제공합니다. 내부 텍스트와 InkWell은 중복 발화되지 않아야 합니다.
- 객체 행은 번호·상품명·확정 상태·confidence를, 후보 행은 상품명·confidence·선택 행동을 단일 레이블로 전달합니다. 현재 검수 항목 제목은 `liveRegion`으로 바뀐 번호를 알립니다.
- 검수 중에는 현재 항목 제목만 알리고 별도 진행 요약을 중복 발화하지 않습니다. 자동 승인 또는 마지막 상품 확인으로 검수가 끝나면 현재 항목 안내를 제거하고 완료 상태와 `최종 확정` 가능 여부를 하나의 `liveRegion`으로 알립니다.
- 단계 탐색기의 이전·다음 버튼은 실제 조작 영역이 각각 44px 이상이어야 하며, 가운데 위치는 `현재 n번 상품, 전체 m개`로 읽고 각 버튼은 `이전 상품 (↑)`·`다음 상품 (↓)`로 목적과 단축키를 함께 전달합니다.
- 프리뷰 box의 축약 레이블과 별개로 Semantics는 `n번`, 현재 선택 여부, 확인 필요 또는 확정 상품명을 모두 전달합니다. 작은 시각 box도 의미 영역과 포인터 영역은 44px 이상이어야 합니다.
- 포커스를 내부 InkWell에만 두지 않고 외부 단일 Semantics 노드의 `focused`에도 동기화합니다.
- 선택 표면은 마우스·터치와 동일하게 Enter와 Space로 작동해야 합니다.
- 후보 또는 전체 상품 검색 결과를 Enter·Space로 확정하면 다음 미확정 객체의 첫 후보로 포커스를 이어갑니다. 마지막 상품이면 같은 위치의 `최종 확정` CTA로 이동합니다. 마우스 클릭은 사용자의 포인터 위치를 존중해 포커스를 강제로 이동하지 않습니다.
- 완료 토스트는 `liveRegion`으로 즉시 알리되되 아이콘·문구가 별도로 중복 발화되지 않게 단일 레이블을 사용합니다.
- reduced motion이 활성화되면 선택 표면의 상태 전환 시간은 `0ms`가 됩니다.

## 반복 개선 체크리스트

각 개선 사이클은 아래 세 관점으로 같은 시나리오를 다시 사용한 뒤 기록합니다.

- **디자이너:** 위계가 3초 안에 읽히는가, 선택·포커스·상태가 서로 다른가, Orange가 필요한 곳에만 있는가
- **PM:** 현재 단계의 Primary가 하나인가, `APPROVED·UNKNOWN·RECAPTURE·ERROR` 계약과 실행 조건이 보존되는가
- **현장 사용자:** 마우스 이동이 짧은가, 1280×720에서도 잘리지 않는가, 다음 행동과 복구 방법을 설명 없이 알 수 있는가

검증 순서는 `준비 → 분석 중 → UNKNOWN 선택 → 최종 확정 → 완료 → Activity 검색·필터·상세 → RECAPTURE → ERROR`입니다. 매 사이클에서 발견한 문제는 먼저 토큰 또는 공통 컴포넌트에서 해결하고 화면별 예외는 마지막 수단으로 사용합니다.

`1440×900 UNKNOWN·선택 후보·단계 탐색 포커스·완료·상품 검색·상품 검색 빈 상태·상품 검색 포커스·Activity 목록·Activity 완료 토스트·Activity 검색 지우기 포커스·Activity 진단 포커스·Activity 진단 상세·Activity 새로고침 포커스·Activity 필터 포커스·Activity 정렬 포커스·Activity 오류`, `1280×720 카메라 확인 중·카메라 미연결 준비·카메라 사용 불가·카메라 촬영 준비·카메라 Secondary 포커스·카메라 Primary 포커스·카메라 촬영 실패·재촬영 중 촬영 실패·재연결 진행·이미지 분석 준비·분석 중·APPROVED·저장 중·저장 중 검수 잠금·저장 실패 복구·완료 포커스·RECAPTURE·재분석형 ERROR·입력 교체형 ERROR·Activity 최초 로딩·Activity 최초 로드 오류·Activity 빈 상태·Activity 결과 없음·새 저장 동기화·새 저장 동기화 실패·상단 탐색 포커스·이미지 폐기 취소 포커스·이미지 폐기 확정 포커스·150% 재촬영 확인·8개 상품 자동 스크롤·밀집 프리뷰·20개 Activity 정렬·Activity 새로고침 중·비차단 새로고침 오류`, `1280×720 125%·150% 큰 글자의 UNKNOWN·Activity, 150% 카메라 촬영 준비와 125% 검색·초기화 상태`, 선택 표면의 Resting·Hover·Selected·Focused·Disabled 상태는 Pretendard와 Material Icons를 실제로 로드한 60개 골든 이미지로 검증합니다. 골든은 단순 업데이트하지 않고 디자이너·PM·현장 사용자 관점의 시각 비평 후에만 갱신합니다.

### 개선 기록

| 사이클 | 관찰 | 개선 |
| --- | --- | --- |
| 1 | 객체·Activity 선택에 관리자 도구형 왼쪽 띠가 혼재 | 전체 선택 면, 사방 외곽선, 체크 아이콘으로 통일 |
| 2 | Activity 진입 직후 `F5`가 동작하지 않음 | 활성 화면 전용 Focus를 연결하고 비활성 화면의 키 가로채기를 차단 |
| 2 | 저장 중 확정 푸터가 사라지고 프리뷰에 별도 비활성 Primary가 표시될 수 있음 | 확정 푸터 하나만 저장 진행을 담당하고 프리뷰는 설명 상태로 전환 |
| 2 | Success·Attention 문구가 옅은 상태 배경에서 4.5:1 미만 | 상태색은 아이콘·경계에 유지하고 배지 문구는 Ink로 변경 |
| 2 | Activity에서 `Ctrl+O` 후 선택 이미지가 보이지 않음 | 선택 완료 시 스캔 작업대로 이동하고 취소 시 현재 화면 유지 |
| 3 | Windows 적응형 compact 밀도가 44/48px 계약을 축소할 가능성 | 표준 밀도와 padded tap target을 Theme에서 강제하고 Windows 플랫폼 크기 테스트 추가 |
| 3 | 선택 표면이 키보드 포커스를 기본 overlay에만 의존 | 선택 여부와 무관하게 사방 2px Orange 포커스 링 표시 |
| 4 | Activity 필터와 프리뷰 overlay가 화면별 스타일 값에 의존 | 공통 FilterGroup과 preview 전용 컴포넌트 색상 역할로 이동 |
| 4 | Theme의 높이·반경이 토큰과 같은 숫자를 별도로 보유 | `AppDesignTokens`의 control/action/panel/pill 값을 Theme과 컴포넌트가 직접 사용 |
| 5 | 선택 행의 외부 Semantics와 내부 InkWell·텍스트가 중복 발화될 가능성 | 단일 Semantics 노드에 레이블·선택·탭 액션을 통합하고 내부 의미 노드 제외 |
| 5 | 포커스만 보이고 키보드 실행 계약이 테스트되지 않음 | Enter·Space가 탭과 동일한 행동을 실행하는지 위젯 테스트로 고정 |
| 5 | 완료 토스트가 시각 표시에만 의존할 수 있음 | 중복 없는 단일 레이블과 `liveRegion`을 제공 |
| 6 | Theme의 역할별 `TextStyle`이 기본 글꼴 속성을 덮어써 실제 Pretendard 상속이 끊김 | `AppTypography` 토큰을 추가하고 모든 핵심 역할을 Pretendard 기반 스타일의 `copyWith`로 구성 |
| 6 | 위젯 테스트가 Ahem 사각형 글리프로 렌더되어 시각 회귀 기준으로 사용할 수 없음 | 공통 테스트 설정에서 Pretendard·Material Icons를 로드하고 4개 대표 운영 상태의 골든 회귀 추가 |
| 6 | `UNKNOWN`·이미지 `RECAPTURE` 시각 픽스처에 이미지가 없어 카메라 연결 상태로 오해됨 | 결정적 테스트 이미지를 넣어 실제 검수·재촬영 구성과 bounding box를 검증 |
| 7 | 이미지 검수가 정상 진행 중인데도 Orange `카메라 확인 필요`가 현재 상태보다 강하게 노출 | 카메라가 이미지 작업을 막지 않을 때는 입력 모드와 미연결을 함께 알리는 중립 배지로 우선순위 하향 |
| 7 | Activity 로드 오류에서도 검색·필터·정렬이 작동할 것처럼 노출 | 오류 상태에서 비작동 제어를 숨기고 `새로고침` 하나에 집중하되 세션 필터 값은 보존 |
| 7 | 준비·분석·저장·완료 상태는 문구 존재만 테스트하고 정보 위계를 시각적으로 검증하지 않음 | 총 10개 운영 상태로 골든 회귀를 확장하고 현장 시나리오와 픽스처를 일치 |
| 8 | 상단 탐색이 기본 InkWell 포커스 효과에만 의존하고 Semantics에 선택·포커스가 명시되지 않음 | 단일 의미 노드에 `button`·`selected`·`focused`·`tap`을 통합하고 2px Orange 사방 링 추가 |
| 8 | 선택 하단선과 사방 포커스선을 하나의 Animated Border로 보간하며 비균일 경계·반경 페인트 오류 발생 | 선택 하단선과 포커스 사방선을 별도 레이어로 분리해 전환 전 구간의 유효한 렌더 보장 |
| 8 | 전역 단축키는 검증했지만 일반 Tab·Enter·Space 운영 경로의 화면 단위 증거가 없음 | `스캔 → 활동` 탐색, Activity 검색·필터 이동과 Space 선택 위젯 테스트, 포커스 골든 추가 |
| 9 | 최소 해상도 검증이 100% 글자와 DPR 1.0에만 제한 | 125%·150% 큰 글자 골든과 Windows DPR 1.5의 1280×720 논리 작업대 위젯 테스트 추가 |
| 9 | 150% 큰 글자에서 후보 confidence의 `75%`가 `75`와 `%`로 두 줄 분리 | 메타 수치 영역을 4px 간격 토큰으로 48px 확보하고 `maxLines: 1`·`softWrap: false`·tabular figures로 단위 보존 |
| 10 | 이미지 입력의 변경 폐기 확인이 카메라 행동인 `다시 촬영`으로 표시되어 결과를 예측하기 어려움 | 입력 방식에 따라 질문과 확정 버튼을 `다른 이미지 선택` 또는 `다시 촬영`으로 일치시키고 위젯·골든 테스트 추가 |
| 10 | 앱 내부 명칭은 `BIXOLON Scanner`인데 MaterialApp·Windows 창 메타데이터는 `Product Scanner`로 노출 | 런타임 앱 제목, 창 제목, Windows 제품 설명을 `BIXOLON Scanner`로 통일 |
| 10 | 확인 대화상자 골든이 Scaffold만 캡처하면 Navigator의 오버레이와 차단막을 검증하지 못함 | `Overlay` 전체를 캡처해 배경 차단, 질문, 영향 설명, 취소·확정 행동을 함께 회귀 검증 |
| 11 | 아직 확인할 상품이 있으면 최종 확정 CTA가 사라져 완료 경로와 푸터 폭이 상태마다 달라짐 | 토스플레이스 다중 선택 템플릿처럼 같은 위치에 남은 수량을 표시한 비활성 CTA를 유지하고 완료 시 수량 포함 확정 CTA로 전환 |
| 11 | 현재 검수·상세 행과 이미 확정된 상품이 모두 체크 아이콘을 사용해 선택과 상태가 겹침 | 값의 확정은 radio/check, 현재 상세 대상은 화살표로 구분하고 전체 선택 면·외곽선은 공통 유지 |
| 11 | Activity에서 카메라 미연결 Attention이 현재 기록 조회보다 강하게 노출 | Activity에서는 중립 `카메라 미연결` 배지로 낮추고 카메라 촬영을 실제로 막는 스캔 상태에서만 Attention 사용 |
| 11 | Activity 상세에서 `확정 완료`·`확정 상품`·`저장됨`이 반복되고 진단 요약에 입력원이 누락 | 패널 제목을 `확정 상품`으로 통합하고 중복 섹션 레이블 제거, 접힌 진단 요약에 입력원과 `모델 버전` 명시 |
| 12 | Windows 선택 행은 클릭 가능하지만 토큰으로 통제되는 전체 면 hover가 없어 마우스 진입 피드백이 약함 | 공통 `AppSelectableSurface`가 `rowHover`와 클릭 커서를 직접 사용하고 Selected가 Hover보다 우선하도록 구현 |
| 12 | `selectionContent`·`disabledSurface`·`disabledContent` 토큰이 선언됐지만 핵심 컴포넌트와 직접 연결되지 않음 | 선택 표면의 기본 콘텐츠와 비활성 FilledButton을 컴포넌트 토큰에 연결해 토큰–구현 단절 제거 |
| 12 | 화면 골든만으로 Resting·Hover·Selected·Focused 상태 차이를 한 번에 비교하기 어려움 | 네 상태를 같은 조건에서 렌더링하는 디자인 시스템 컴포넌트 골든과 hover·우선순위 위젯 테스트 추가 |
| 13 | 모든 `ERROR`의 패널 제목이 `연결 확인`이고 입력 형식·용량 오류에도 같은 이미지를 `다시 시도`하도록 안내 | 내부 `ScannerErrorRecovery`로 입력 교체형과 재분석형을 구분하고 원인별 아이콘·제목·Primary 행동 연결 |
| 13 | 오류 행동인 `다시 시도`가 실제로 재연결·재촬영·재분석 중 무엇을 하는지 설명하지 못함 | 현재 이미지를 다시 보내는 행동은 `다시 분석`, 입력 교체는 `다른 이미지 선택` 또는 `다시 촬영하기`로 명명 |
| 13 | 입력 이미지 `ERROR`와 서버 `ERROR`가 같은 cloud 아이콘을 사용하고 `RECAPTURE`와의 차이는 색상에 크게 의존 | 손상 이미지·연결 끊김·촬영 가이드 아이콘과 서로 다른 교정 문구를 사용하고 세 상태 골든을 나란히 비평 |
| 14 | 초기 준비 화면이 입력 상황과 관계없이 `분석 결과`·`아직 분석된 상품이 없어요`를 표시해 현재 단계보다 결과 부재를 강조 | 카메라 확인 중·입력 준비·분석 준비·분석 중으로 헤더와 본문을 분리하고 다음 행동을 능동형 문구로 안내 |
| 14 | 원래 검증 순서에 `준비`가 포함됐지만 카메라 초기화·미연결·이미지 준비의 화면 단위 시각 증거가 없음 | 세 진입 상태를 1280×720 골든으로 추가하고 각 상태의 Primary 수·활성 조건·최소 높이를 위젯 테스트로 고정 |
| 14 | 지속적으로 회전하는 카메라 초기화 표시를 `pumpAndSettle`로 검증하면 테스트가 종료되지 않음 | 애니메이션을 제거하지 않고 결정적인 한 프레임을 캡처해 초기화 피드백과 레이아웃을 안정적으로 회귀 검증 |
| 15 | UNKNOWN 골든이 후보 2개만 사용해 실제 Top-3의 높이·정보 밀도·큰 글자 동작을 증명하지 못함 | 세 후보를 모두 60px 토큰 행으로 렌더하고 100%·125%·150% 골든에서 상품명 우선, class ID·confidence 보조 위계를 검증 |
| 15 | 후보를 확정하면 다음 객체는 선택되지만 상품이 많은 목록에서는 선택 행이 화면 밖에 남을 수 있음 | 후보 확정·목록 클릭·방향키 이동 시 결과 목록을 현재 행 중심으로 자동 스크롤하고 8개 객체 최소 화면 골든과 위치 테스트 추가 |
| 15 | 후보 영역의 `어떤 상품인가요?`만으로는 어느 객체인지와 선택 후 동작을 알기 어려움 | `n번 상품을 확인해 주세요`와 `선택하면 다음 확인 항목으로 이동해요`로 항목·결과를 명시하고 변경 상태는 별도 문구로 구분 |
| 15 | 단일 Semantics가 확정 여부만 읽어 상품명·confidence를 생략 | 객체·후보 레이블에 번호, 상품명, confidence, 상태를 포함하고 현재 검수 제목을 live region으로 제공 |
| 16 | Activity 상단의 `최근 n건 · 표시 n건`, 목록 수량, 상세 상품 수가 한 화면에서 중복되어 정보 위계가 약함 | 결과 수량을 목록 헤더 한 곳으로 모으고 조건 적용 시에만 `검색 결과`와 `표시/전체` 수량을 함께 제공 |
| 16 | 검색 결과가 남아 있으면 검색·필터·정렬을 한 번에 되돌릴 방법이 없고 검색어 삭제도 키보드 편집에 의존 | 입력 필드의 `검색어 지우기`와 조건이 활성화된 동안 유지되는 `모두 초기화` 행동 추가 |
| 16 | 여러 상품 기록이 쉼표 문자열로 잘리고, `Scon` 검색 결과가 `Muffin 외 2개`로 보여 일치 이유를 설명하지 못함 | 기본은 `첫 상품 외 n개`, 상품명 검색 중에는 일치 상품을 요약 첫 항목으로 올리고 상세에 전체 목록 유지 |
| 16 | Activity 행의 단일 Semantics가 날짜만 전달하고 상품·입력원·검수 결과를 생략 | 날짜·시각·상품 요약·입력원·`자동 확정/n개 수정`을 하나의 활동 기록 레이블로 통합 |
| 16 | 큰 글자 골든이 여러 해상도 테스트 뒤 실행될 때 Flutter 렌더 표면 간섭으로 부분 캡처될 수 있음 | 125%·150% 전환 시나리오를 독립 테스트 파일로 격리하고 검색·초기화 125% 골든을 추가 |
| 17 | Activity 새로고침이 기존 목록·상세를 전체 스피너로 바꿔 작업 맥락과 저장 기록이 잠시 사라짐 | 최초 로딩과 백그라운드 새로고침을 분리하고 기존 기록이 있으면 버튼 위치 진행 표시만 사용 |
| 17 | 기존 기록이 있는 새로고침 실패도 전체 오류 화면으로 전환해 사용 가능한 기록을 가림 | 기존 목록·선택·상세를 유지한 Error Soft 인라인 알림과 44px 복구 행동 제공, 최초 실패만 전체 오류 유지 |
| 17 | 진행 중 `F5`를 반복하면 동일한 기록 요청을 중복 실행할 수 있음 | 새로고침 중 버튼과 `F5`를 같은 로딩 가드로 차단하고 요청 횟수 테스트 추가 |
| 17 | 20개 기록에서 정렬을 바꾸면 유지된 선택 행이 목록 반대편 화면 밖으로 이동 | 선택 ID와 정렬 후 index를 함께 추적해 현재 행을 자동 스크롤하고 1280×720 골든·위치 테스트 추가 |
| 18 | 밀집 객체에서 현재 선택 box가 응답 순서상 뒤에 있는 다른 box와 레이블 아래에 가려질 수 있음 | 비선택 box를 먼저, 현재 선택 box를 마지막에 그려 Brand 경계·면·`현재 검수` 레이블을 항상 최상단에 유지 |
| 18 | 작은 검출 box의 클릭·Semantics 영역이 44px보다 작아 현장 포인터 선택이 어려움 | 모델 좌표 경계는 바꾸지 않고 이미지 영역 안에서 상호작용 rect만 최소 44px로 확장해 모든 box 크기 테스트 |
| 18 | 모든 확정 box가 상품명을 반복하고 미확정 box가 `확인 필요`를 반복해 밀집 프리뷰가 레이블로 가려짐 | 비선택 확정은 `n`, 확인 필요는 `n ?`, 선택은 `n 현재 검수`로 축약하고 전체 의미는 목록·Semantics에 유지 |
| 18 | 프리뷰 번호가 `item_id` 뒤 숫자 파싱에 의존해 비숫자 ID에서 `0`으로 표시될 수 있음 | 응답 배열의 실제 index를 전달해 결과 목록 번호와 일치시키고 비숫자 ID 밀집 테스트 추가 |
| 18 | 프리뷰 단계가 검수 진행량을 보여주지 않아 수량 확인을 위해 오른쪽 패널로 시선을 이동 | `상품 검수 n/전체 → 상품 검수 완료` 단계 문구를 프리뷰 상단 동일 위치에서 전환 |
| 19 | Activity 목록의 처리시간이 확정 결과보다 먼저 보여 현장 사용자의 기록 비교 기준과 맞지 않음 | 마지막 열을 `검수 결과`로 바꾸고 아이콘·문구로 `자동 확정/n개 수정`을 즉시 구분 |
| 19 | 확정 상품마다 item ID·confidence·확정 방식이 반복되어 상품명과 수정 여부의 위계가 약함 | 기본 상세는 상품명·`수정됨`만 유지하고 기술 값은 접힌 객체별 판정으로 이동 |
| 19 | 접힌 진단 정보의 객체별 판정은 동작 테스트만으로 실제 밀도와 스크롤 상태를 검증하기 어려움 | 1440×900 진단 상세 골든을 추가하고 1280×720 150% Activity에서 기본 정보 위계와 오버플로를 재검증 |
| 20 | 문서의 4px 간격 계약과 달리 배지·탐색·검수·Activity에 6·7·9·10·11·14·18px 화면별 간격이 남음 | 레이아웃 패딩과 요소 간 간격을 `AppSpacing`의 4·8·12·16·24·32px 역할로 정규화 |
| 20 | Theme 버튼·입력 패딩이 토큰과 같은 의미의 숫자를 직접 보유해 컴포넌트와 화면 리듬이 분리됨 | Filled·Outlined·Text button과 Input의 내부 패딩을 `AppSpacing`에 직접 연결 |
| 20 | 최소 크기 테스트는 있었지만 간격 토큰과 Theme 연결의 정적 계약이 없었음 | 모든 spacing 값의 4px 배수와 Theme resolved padding을 검증하는 토큰 테스트 추가, 28개 골든을 새 리듬으로 재비평 |
| 21 | 이전·다음 객체 이동이 분리된 아이콘 버튼뿐이라 현재 위치와 전체 수량을 즉시 알기 어려움 | 두 행동과 `현재/전체`를 하나의 단계 탐색기로 묶어 `이전 | n/전체 | 다음` 구조로 제공 |
| 21 | 모든 객체를 확인해 선택이 해제된 뒤에도 이동 행동이 남으면 최종 확정 CTA와 시각적으로 경쟁 | 선택된 다중 객체가 있을 때만 단계 탐색기를 노출하고 단일 객체·완료·APPROVED에서는 숨김 |
| 21 | 44px 외곽 컨테이너에 1px 경계를 더하면 내부 IconButton의 실제 조작 영역이 42px로 축소 | 외곽 높이를 48px action 토큰에 연결하고 두 버튼의 실제 렌더 높이가 각각 44px 이상인지 위젯 테스트로 검증 |
| 22 | 저장 기록이 0건인데도 검색·필터·정렬이 모두 보여 작동하지 않는 도구가 빈 상태의 다음 행동보다 먼저 노출 | 데이터가 있을 때만 Activity 도구를 공개하고 빈 상태에는 `스캔 화면으로 이동` 단일 Primary를 제공 |
| 22 | 최초 Activity 로드 오류에서 화면 제목이 사라지고 유일한 복구 행동도 Secondary로 표시 | `활동 기록` 제목을 유지하고 `새로고침`을 단일 Primary로 격상하되 기존 기록이 있는 비차단 오류는 기존 방식 유지 |
| 22 | 검색 필드의 일반 키보드 아이콘이 실제 `/` 단축키를 설명하지 못하고 150%에서 긴 placeholder가 잘림 | 짧은 `상품명 또는 Scan ID` 문구와 24px `/` 키캡을 사용하고 44px suffix 슬롯·Semantics·명시적 첫 Tab 순서를 검증 |
| 23 | 카메라 초기화·미연결·이미지 준비는 골든이 있지만 실제 기본 경로인 카메라 연결 성공의 `촬영 준비` 화면 증거가 없음 | 결정적 라이브 프리뷰 픽스처를 추가하고 상태 배지·프리뷰·패널·단일 `촬영하기` Primary를 100%·150% 골든과 위젯 테스트로 검증 |
| 24 | 카메라가 연결된 뒤 촬영 호출만 실패하면 `cameraMessage`는 생기지만 연결 성공 배지·`촬영 준비`·`촬영하기`가 그대로 남아 실패와 복구 행동이 보이지 않음 | 상단 배지·프리뷰·헤더·본문을 `카메라 확인 필요` 복구 상태로 일치시키고 활성 Primary를 `다시 연결` 하나로 교체 |
| 24 | 로컬 카메라 실패를 Worker `ERROR` 또는 모델 `RECAPTURE`처럼 표현하면 공개 상태 계약과 원인 도메인이 섞임 | `processState: ready`·응답 `null`을 유지하는 Flutter 내부 복구 상태로 분리하고 촬영 실패·재연결 성공 회귀 테스트 추가 |
| 24 | 실패 제목·상세·버튼이 모두 재연결을 반복하면 원인과 다음 행동의 정보 위계가 약함 | 제목은 사건인 `촬영하지 못했어요`, 상세는 원인·복구 안내, 버튼은 실제 행동인 `다시 연결`로 역할을 분리 |
| 25 | `RECAPTURE` 뒤 재촬영 호출이 실패하면 상단은 카메라 문제를 알리지만 프리뷰·패널·CTA는 이전 재촬영 판정을 계속 표시 | `hasActiveCameraIssue`를 공통 우선순위로 두고 이전 응답은 보존하되 모든 상태 표면과 단일 Primary를 `카메라 확인 필요`·`다시 연결`로 전환 |
| 25 | Activity 비차단 새로고침 오류의 행동이 `다시 시도`라 무엇을 다시 실행하는지 문맥에 의존 | 실패 문구와 버튼을 `새로고침하지 못했어요`·`새로고침`으로 연결해 토스플레이스식 직접 행동 명명 적용 |
| 26 | `다시 연결`을 누르면 메시지가 먼저 지워져 비동기 연결이 끝나기 전에 이전 `RECAPTURE`와 활성 `다시 촬영하기`가 다시 노출 | 연결 진행을 `isCameraCheckActive` 현재 상태로 승격해 모든 표면을 `카메라 확인 중`으로 유지하고 CTA를 비활성 `연결 확인 중`으로 전환 |
| 26 | 연결 진행 중 컨트롤러 메서드를 다시 호출하면 카메라 초기화가 중복 실행될 수 있음 | `cameraInitializing`·busy 가드로 중복 호출을 차단하고 지연 Future 테스트에서 초기화 호출이 1회인지 검증 |
| 27 | 초기 카메라 연결 실패와 촬영 호출 실패가 같은 문자열 상태에 합쳐져 촬영한 적이 없어도 `촬영하지 못했어요`로 표시 | 내부 `CameraIssueType.unavailable/captureFailed`를 추가하고 각각 `카메라를 사용할 수 없어요`·`촬영하지 못했어요`로 사건 제목 분리 |
| 27 | 카메라 사용 불가 상태는 수동 미연결 골든만 있어 실제 초기화 예외의 문구·Primary 수·재연결 실패 유지가 검증되지 않음 | 초기화 예외 Gateway로 1280×720 상태를 렌더하고 단일 `다시 연결`, 이미지 대체 경로, 반복 실패 상태를 위젯·골든으로 고정 |
| 28 | 버튼은 `다시 분석`·`새로고침`으로 구체화됐지만 오류 상세와 내부 fallback에는 `다시 시도`가 남아 화면 어휘가 분리 | 서버·시간 초과는 `다시 분석`, Activity 로드는 `새로고침`, 일반 재촬영은 입력원별 `다시 촬영/다른 이미지 선택`으로 본문과 CTA 동사 통일 |
| 28 | HTTP 상태가 200이면 본문 상태가 `ERROR`여도 성공 결과처럼 회귀할 위험을 별도 테스트로 증명하지 않음 | 최상위 `ERROR`는 HTTP 코드와 무관하게 `ScannerApiException`·재분석 복구가 되는 계약 테스트 추가 |
| 29 | Top-3는 공통 선택 표면을 사용하지만 전체 상품 검색 결과만 48px raw InkWell이라 hover·포커스 링·통합 Semantics 계약이 끊김 | 검색 결과도 `_ProductChoice` 60px 전체 면으로 통합해 상품명 우선, class ID 보조, hover·포커스·Enter 실행을 동일하게 적용 |
| 29 | 상품 검색 결과가 없으면 `일치하는 상품이 없어요` 한 줄만 표시해 다음 입력 방법과 상태 의미가 약함 | 검색 없음 아이콘·제목·`검색어 또는 class ID 변경` 안내를 하나의 live region으로 제공 |
| 29 | 포커스 직후 첫 애니메이션 프레임을 캡처하면 의미 포커스는 이동했지만 이전 입력 테두리가 남아 실제 안정 상태를 증명하지 못함 | 포커스 모션 완료 후 결과 행의 Orange 사방 링을 캡처하고 Tab·Enter 확정까지 위젯 회귀로 고정 |
| 30 | 문서에 정의한 결과 패널 비율·최소/최대 폭과 검수·검색 영역 크기가 화면별 숫자로 남아 디자인 시스템과 구현이 분리 | 결과 패널 `36%·440~520px`, 검수 영역 `340px`, Activity 검색창 `320px`, 적층 전환점 `960px`를 `AppDesignTokens`·`AppBreakpoints`로 승격 |
| 30 | 토큰 선언값만 검사하면 실제 레이아웃이 토큰을 우회하거나 부모 제약에서 달라져도 발견하기 어려움 | `1280×720`·`1440×900` 결과 패널 실제 폭, 검수 최대 높이, Activity 검색창 폭을 위젯 테스트로 측정하고 레이아웃 토큰의 4px 리듬을 함께 검증 |
| 31 | 객체가 1~2개여도 목록이 남은 세로 공간을 모두 차지해 선택 행과 해당 Top-3 사이에 큰 빈 공간이 생김 | 객체 목록을 실제 행 높이만큼 축소하고 선택 행 바로 아래에 검수 영역을 연결해 제목→대상→선택지의 연속 흐름 구성 |
| 31 | 다수 객체에서 목록을 우선하면 현재 객체는 보여도 Top-3 마지막 행동인 `다른 상품 검색`이 푸터 뒤에 일부 가려질 수 있음 | 검수 영역 최대 높이 `340px`를 우선 확보하고 객체 목록만 스크롤해 8개 객체·`1280×720`에서도 현재 행, Top-3, 검색 행동을 동시에 노출 |
| 31 | 시각적으로 가까워졌다는 골든만으로 선택 행과 검수 영역 사이의 실제 간격·잘림 여부를 정량 증명하기 어려움 | 목록 프레임 하단과 검수 영역 상단이 정확히 맞닿는지, 검색 행동 하단이 검수 viewport 안에 있는지 위젯 좌표로 검증 |
| 32 | Activity 정렬 뒤 선택 기록을 중앙에 맞춘 소수점 scroll offset 때문에 컬럼 헤더 아래 첫 기록이 행 중간부터 잘려 보임 | 선택 기록 자동 이동을 `60px` 행 index 기반 offset으로 정렬해 표의 첫 가시 행을 완전한 단위로 표시 |
| 32 | 마지막 기록을 완전히 보이게 하려고 max scroll에 맞추면 viewport 나머지 높이 때문에 다시 비정렬 offset이 발생 | viewport 높이를 행 높이로 나눈 나머지만 하단 buffer로 추가해 마지막 선택 행과 상단 첫 행을 동시에 완전 노출 |
| 32 | 선택 행이 보인다는 기존 검사만으로 첫 가시 행의 잘림은 발견하지 못함 | `1280×720` 오래된순 20건에서 14번 행 상단이 목록 viewport 상단과 일치하고 20번 선택 행 하단 여유가 한 행 미만인지 좌표 검증 |
| 33 | `headerHeight: 60` 토큰이 선언됐지만 공통 `AppPanelHeader`는 별도 `56px`를 사용해 상단 셸과 패널 리듬이 불일치 | 공통 패널 헤더의 최소 높이를 `AppDesignTokens.headerHeight`에 직접 연결하고 실제 스캔 결과 헤더가 60px인지 검증 |
| 33 | 공통 섹션 라벨과 Activity 표 헤더가 같은 40px 역할을 각각 숫자로 복제 | `sectionHeaderHeight: 40`을 ThemeExtension에 추가하고 `AppSectionLabel`·Activity 컬럼 헤더를 동일 토큰에 연결 |
| 33 | 토큰 값 비교만으로 공통 컴포넌트가 토큰을 실제 소비하는지는 증명되지 않음 | 공통 패널·섹션 헤더와 스캔·Activity 화면의 실제 렌더 높이를 위젯 테스트로 측정하고 37개 운영 골든을 재비평 |
| 34 | 동일한 카메라 재촬영 행동이 검수 폐기에서는 `다시 촬영`, 모델·입력 오류 복구에서는 `다시 촬영하기`로 달라 결과 예측 언어가 분리 | 검수 폐기 대화상자, `RECAPTURE`, 입력 교체형 `ERROR`의 버튼·Tooltip을 `다시 촬영`으로 통일 |
| 34 | 고정 확정 CTA의 계획상 진행 상태는 `저장 중`인데 실제 버튼은 `저장하고 있어요`를 사용 | 제출 진행 문구를 `저장 중`으로 맞춰 `남은 상품 확인 필요 → n개 상품 최종 확정 → 저장 중` 상태 어휘 계약 복원 |
| 34 | 핵심 행동 문자열이 화면별 리터럴이라 향후 같은 작업의 이름이 다시 달라질 수 있음 | `AppActionCopy` 콘텐츠 토큰에 입력 선택·촬영·분석·연결·새로고침·저장 문구를 중앙화하고 단위·상태·골든 테스트로 고정 |
| 35 | Activity 필터의 현재 값이 저농도 Orange 배경만으로 구분돼 색각 차이와 밝은 모니터에서 선택 상태가 약함 | `AppFilterGroup`의 선택 칩에 전체 1.5px `selectionOutline`과 Bold 레이블을 함께 적용하고 선택 Semantics를 유지 |
| 35 | 선택 표시를 위해 체크를 추가하면 150% 큰 글자에서 7개 필터 폭이 늘어나고 값 전환마다 주변 제어가 이동 | 체크나 왼쪽 띠 없이 면·사방 경계·굵기 세 신호를 사용하고 100%·125%·150% Activity 골든으로 무오버플로 확인 |
| 36 | Activity 정렬 드롭다운은 Tab으로 도달할 수 있지만 수동으로 감싼 외곽선이 포커스를 반영하지 않아 현재 위치를 시각적으로 알 수 없음 | `AppDropdownControl`로 추출해 `controlHeight`·`controlRadius`·`focusRing`·`motionFast` 토큰을 연결하고 포커스 시 사방 2px Orange 링 표시 |
| 36 | 공통 컴포넌트 단위 포커스 검증만으로 실제 Activity Tab 순환에서 정렬까지 도달하는지는 증명되지 않음 | 검색·필터 선택 뒤 정렬에 도달할 때까지 실제 Tab을 보내 Focus Semantics와 외곽선을 함께 검증하고 1440×900 포커스 골든 추가 |
| 36 | 포커스 애니메이션을 추가하면 Windows 접근성의 애니메이션 끄기 설정을 무시할 수 있음 | `MediaQuery.disableAnimations`일 때 드롭다운 포커스 전환을 `Duration.zero`로 고정 |
| 37 | Activity 필터는 선택값의 1.5px 외곽선은 명확하지만 Tab 포커스가 Material의 옅은 overlay에 의존해 선택과 현재 키보드 위치를 구분하기 어려움 | `AppFilterChip`으로 추출하고 포커스일 때 선택 여부보다 우선하는 사방 2px `focusRing` 적용 |
| 37 | 필터 포커스 단위 테스트만으로 검색창에서 실제 Tab 이동 뒤 화면상 단일 포커스가 유지되는지는 증명되지 않음 | 검색창의 이전 포커스 애니메이션이 끝난 뒤 비선택 카메라 필터의 Focus Semantics·2px 경계·1440×900 골든을 함께 검증 |
| 37 | 포커스 표식을 별도 아이콘이나 왼쪽 띠로 추가하면 150% 큰 글자에서 필터 폭과 정렬이 흔들림 | 칩의 면·문구·크기는 유지하고 경계 두께 우선순위만 `Focused 2px > Selected 1.5px > Resting 1px`로 정의 |
| 38 | Activity의 입력원과 기간에 모두 `전체`가 있어 시각 그룹을 보지 못하는 사용자는 포커스된 조건을 이름만으로 구분할 수 없음 | `AppFilterChip.semanticLabel`을 필수화하고 `입력원, 전체`·`기간, 전체`처럼 그룹 문맥과 값을 결합 |
| 38 | ChoiceChip 기본 Semantics와 외부 보강 Semantics를 함께 노출하면 같은 레이블·선택·탭 정보가 중복 발화될 수 있음 | 내부 Semantics를 제외하고 단일 노드에 `button`·`selected`·`focused`·상호 배타 그룹·`tap` 정보를 통합 |
| 38 | 컴포넌트 단위 레이블 검사만으로 실제 화면의 두 `전체`가 고유한지는 증명되지 않음 | Activity 화면에서 `입력원, 전체`와 `기간, 전체`가 각각 정확히 하나이고 단독 `전체` 의미 노드는 없는지 회귀 검증 |
| 39 | `AppInlineNotice`가 메시지 중복을 막으려고 전체 자식 Semantics를 제외해 인접한 `새로고침` 버튼과 탭 액션까지 보조기기에서 사라짐 | 알림 컨테이너를 명시적 자식 노드 구조로 바꾸고 메시지 live region과 복구 버튼 Semantics를 분리 |
| 39 | 상태 아이콘까지 의미 트리에 남기면 오류 문구 앞에 장식 정보가 반복되고 메시지의 즉시성이 낮아짐 | 아이콘만 `ExcludeSemantics`하고 오류 문구는 단일 live region으로 유지 |
| 39 | 공통 컴포넌트의 노드 구조만으로 실제 복구 실행과 목록 보존까지 증명되지 않음 | Activity 새로고침 실패에서 독립 `새로고침` 버튼·tap을 확인하고 실행 후 4번째 로드, 알림 제거, 기존 상품 유지까지 검증 |
| 40 | Activity 최초 로드에서 데이터가 없는데도 검색·필터·정렬이 먼저 노출돼 작동 가능한 도구처럼 보임 | 최초 로드 중에는 `활동 기록` 헤더만 유지하고 데이터가 도착한 뒤 도구를 공개하는 단계적 정보 공개 적용 |
| 40 | 빈 작업면의 작은 스피너만으로 무엇을 기다리는지 추측해야 하고 보조기기에도 구체적 로딩 대상이 없음 | 공통 `AppLoadingState`에 Orange 진행 표시, `활동 기록을 불러오는 중이에요` 가시 문구와 단일 live region 제공 |
| 40 | 정적 첫 프레임 골든은 진행 표시를 작은 점처럼 캡처하고 클릭 ripple까지 남겨 실제 로딩 상태를 왜곡 | 600ms 진행 프레임에서 transient 클릭 효과가 사라지고 스피너 arc가 보이는 1280×720 골든을 승인 |
| 40 | 기존 기록이 있는 새로고침까지 전체 로딩 화면으로 바꾸면 현재 목록·상세와 작업 맥락이 사라짐 | `_logs.isNotEmpty` 경로는 기존 도구·목록·상세와 버튼 내 진행 표시를 그대로 유지하고 최초 빈 로드만 분리 |
| 41 | 저장 중 CTA는 시각적으로만 `저장 중`으로 바뀌고 비활성 버튼의 기본 Semantics에 의존해 상태 전환이 자동 안내된다는 보장이 약함 | 공통 `AppProgressActionButton`이 위치·크기·진행 아이콘을 유지하면서 `저장 중. 완료될 때까지 기다려 주세요` 단일 live region을 제공 |
| 41 | 진행 상태를 별도 활성 버튼으로 노출하면 중복 제출이나 같은 화면의 Primary 경쟁으로 오해할 수 있음 | 진행 노드는 button 역할을 유지하되 disabled·tap action 없음으로 명시하고 실제 `FilledButton.onPressed`도 `null`인지 통합 테스트로 검증 |
| 41 | 애니메이션 첫 프레임 골든은 20px 진행 표시를 점처럼 캡처해 실제 상태를 충분히 보여주지 못함 | 상태 전환 600ms 뒤 동일 1280×720 저장 중 골든을 캡처하고 고정 CTA·보조 설명·단일 Primary 구조를 재승인 |
| 42 | 프리뷰와 결과 패널은 `분석 중`인데 하단 고정 CTA만 비활성 `분석하기`로 남아 현재 상태와 실행 전 행동이 충돌 | 분석 시작 시 같은 CTA를 진행 아이콘과 `분석 중`으로 전환하고 이미지·카메라 입력 모두 공통 `AppProgressActionButton` 사용 |
| 42 | 카메라 확인 CTA는 `연결 확인 중` 문구만 표시해 보조기기가 비동기 상태 진입을 자동으로 알 수 없음 | `카메라 연결 확인 중. 완료될 때까지 기다려 주세요` 단일 live region과 disabled·tap 없음 상태를 제공 |
| 42 | 정적 첫 프레임의 여러 스피너가 점처럼 보이면 화면별 진행 피드백의 일관성을 시각적으로 판단하기 어려움 | 분석·최초 카메라 확인·재연결 골든을 각각 600ms 진행 프레임에서 캡처해 프리뷰·본문·고정 CTA의 동기화를 재승인 |
| 42 | 같은 골든이 단독 실행에서는 이미지 디코딩 전, 전체 실행에서는 캐시된 이미지로 캡처돼 진행 UI와 무관한 큰 차이가 발생 | 이미지 기반 진행·복구 골든 전에 테스트 이미지를 명시적으로 precache해 단독·파일 전체·전체 suite가 같은 프리뷰를 검증 |
| 43 | Activity 새로고침은 Tab 순서에는 포함되지만 옅은 기본 overlay에 의존해 현재 키보드 위치가 선택 필터·정렬보다 약함 | 공통 `AppIconActionButton`으로 48px 조작면과 포커스 시 사방 2px Brand 링을 적용하고 실제 검색 → 새로고침 Tab 이동 골든 추가 |
| 43 | 아이콘 행동에 항상 경계나 면을 두면 검색창 옆 보조 행동이 Primary처럼 무거워지고 토스형 미니멀 위계가 흐려짐 | Resting에서는 표면을 추가하지 않고 Focused에서만 링을 표시하며 hover·Tooltip은 기존 방식 유지 |
| 43 | 백그라운드 새로고침 스피너는 시각적으로만 바뀌어 보조기기가 작업 시작과 중복 실행 차단을 알기 어려움 | `활동 기록 새로고침 중. 완료될 때까지 기다려 주세요` 단일 live region, button·disabled·tap 없음 상태를 공통 아이콘 행동에 통합 |
| 43 | 툴바 새로고침과 오류 알림의 `새로고침`이 같은 의미 이름이면 보조기기 검색에서 두 행동을 구분하기 어려움 | 툴바는 `활동 기록 새로고침`, 인라인 복구는 `새로고침`으로 대상 문맥을 분리하고 각 노드의 tap 유무를 검증 |
| 44 | 기본 `ExpansionTile`의 접힘은 작동하지만 앱의 2px 포커스 규칙과 모션 토큰을 직접 보장할 수 없음 | 공통 `AppDisclosure`로 60px 헤더, 사방 Brand 포커스 링, `expanded` Semantics와 Enter·Space 실행을 통합 |
| 44 | 포커스 때 Material Orange overlay와 Brand 링을 함께 쓰면 현재 위치가 선택 상태처럼 보임 | 기본 focus overlay를 제거하고 면은 중립으로 유지한 채 사방 2px 링만 표시해 선택·포커스 의미를 분리 |
| 44 | `AnimatedSize(duration: 0)`은 reduced motion 테스트에서 자체 레이아웃 mutation을 일으킬 수 있음 | reduced motion에서는 애니메이션 시간을 0으로만 바꾸지 않고 `AnimatedSize`를 트리에서 완전히 제거 |
| 44 | 마우스 클릭만으로 진단 상세를 검증하면 실제 Activity Tab 순서와 확장 상태 전달을 증명하지 못함 | 실제 Tab 순환으로 `진단 정보`에 도달해 포커스 골든을 캡처하고 Enter 확장, `collapsed → expanded` Semantics, 상세 스크롤을 함께 검증 |
| 45 | 새로고침만 공통 아이콘 행동을 사용하고 단계 이동·검색 지우기·검색 닫기는 기본 Material 포커스 표현에 남아 같은 앱에서 키보드 위치의 강도가 달라짐 | 모든 아이콘 단독 행동을 48px `AppIconActionButton`으로 통일하고 Tooltip·단일 Semantics·사방 2px Brand 링을 공유 |
| 45 | 단계 탐색기 외곽선이 레이아웃 padding으로 계산되어 48px 토큰 안의 이전·다음 조작면이 실제로 46px까지 축소됨 | 외곽선을 자식 제약과 분리한 `DecoratedBox` 구조로 바꿔 두 행동의 렌더 크기를 정확히 48px로 고정 |
| 45 | 포커스 직후 첫 프레임 골든은 시맨틱 포커스만 이동하고 160ms Brand 링 전환이 끝나지 않아 실제 시각 위치를 증명하지 못함 | 전환 완료 뒤 단계 이동·Activity 검색 지우기 포커스를 캡처하고 Orange 링을 육안 비평한 1440×900 골든 2종 추가 |
| 45 | 마우스 tap만으로 검색 지우기·검색 닫기를 검증하면 텍스트 입력에서 아이콘 행동으로 이동하는 실제 키보드 경로가 빠짐 | Activity는 TextField → Tab → Space, 상품 검색은 TextField → Shift+Tab → Enter 경로와 focused Semantics·링·실행 결과를 통합 검증 |
| 46 | 검수 내용을 버리는 확인이 기본 `AlertDialog`의 작은 텍스트 취소와 암묵적인 포커스 순서에 의존해 안전한 첫 행동을 예측하기 어려움 | 400px `AppConfirmDialog`로 분리하고 취소·실행을 같은 48px 행동으로 제공하며 `취소`를 명시적인 초기 포커스로 고정 |
| 46 | `현재 확인한 내용이 사라져요`는 무엇을 잃는지 사용자가 다시 해석해야 함 | `지금까지 확인한 상품 선택이 사라져요`로 손실 대상을 구체화하고 제목의 이미지 교체·재촬영 동사를 실행 버튼까지 유지 |
| 46 | Orange Primary 위에 같은 Orange 포커스 링을 그리면 상태는 존재해도 시각적으로 사라짐 | 중립 취소는 Brand 링을 유지하고 Orange Primary에는 4.5:1 이상 대비의 Ink 2px 링을 사용하는 접근성 예외 적용 |
| 46 | 마우스 취소만 검증하면 안전한 기본값, 실행 행동 이동, 대화상자 닫기, 150% 글자 확대를 증명하지 못함 | `초기 취소 → Tab → 실행`, Esc 취소, 취소 Enter, 실행 Enter와 150% 1280×720 무오버플로를 위젯·골든으로 통합 검증 |
| 47 | 완료 토스트가 스캔 작업대 내부에 있어 사용자가 결과 확인을 위해 Activity로 이동하면 3초 수명 전에 즉시 사라짐 | 상단 셸 아래의 전역 feedback 레이어로 이동해 `IndexedStack` 화면 전환 중에도 같은 메시지·위치·live region을 유지 |
| 47 | 완료 유지 시간이 컨트롤러의 하드코딩된 `Future.delayed`라 연속 완료 시 이전 비동기 작업이 새 메시지를 조기에 지울 수 있음 | `AppMotion.feedbackHold = 3초` 토큰과 취소 가능한 단일 `Timer`를 사용하고 새 완료·dispose에서 이전 타이머를 정리 |
| 47 | 수동으로 주입한 완료 문구만 테스트하면 실제 저장 성공→세션 초기화→토스트→만료 경로를 증명하지 못함 | 실제 두 상품 확정·저장 후 Scan에서 live region을 확인하고 Activity 이동 후 유지, 토큰 시간 경과 후 제거까지 위젯 clock으로 검증 |
| 47 | 어두운 프리뷰에서 보이지 않던 elevation 6 그림자가 밝은 Activity에서는 두꺼운 회색 포커스 링처럼 보임 | 비조작 토스트의 elevation을 3과 16% Ink shadow로 낮추고 밝은 오류 화면 골든에서 포커스와 혼동되지 않는지 재비평 |
| 48 | 주요 Filled·Outlined·Text 버튼은 Tab으로 도달해도 전역 Orange focus overlay에 의존해 Orange CTA에서 현재 위치가 거의 보이지 않음 | Theme 버튼 상태 토큰에 Filled용 Ink 2px, Outlined·Text용 Brand 2px 경계를 연결하고 focus overlay는 투명하게 유지 |
| 48 | 대화상자에만 있던 Primary 포커스 예외로는 촬영·분석·재연결·최종 확정 등 실제 운영 CTA의 일관성을 보장할 수 없음 | 모든 Theme 버튼에 같은 역할 규칙을 적용하고 1280×720 촬영 준비에서 실제 Tab으로 Secondary→Primary를 이동해 focused Semantics와 골든 2종을 검증 |
| 48 | 포커스 표현이 버튼 크기나 고정 Action bar의 배치를 바꾸면 최소 작업대에서 버튼이 흔들려 반복 조작 정확도가 낮아짐 | 경계를 ButtonStyle 상태로 렌더링해 44·48px 크기와 레이아웃을 유지하고 두 포커스 골든을 나란히 시각 비평 |
| 49 | 분석 화면에서 `이미지 분석 중`·`분석 중`·`분석하고 있어요`·진행 CTA가 같은 상태를 네 번 반복해 입력 출처와 다음 행동의 위계가 흐려짐 | 토스 프론트 Template의 title·description·CTA 역할 분리를 적용하고 프리뷰 칩을 단계가 아닌 입력 출처 전용으로 재정의 |
| 49 | 검수·오류로 전환할 때 프리뷰 칩까지 상태 문구로 바뀌어 사용자가 카메라 촬영본인지 파일 입력인지 화면 안에서 다시 확인할 수 없음 | `AppPreviewCopy`에 `라이브 카메라·촬영 이미지·선택한 이미지·카메라 미리보기`를 콘텐츠 토큰화하고 모든 처리 상태에서 출처를 안정적으로 유지 |
| 49 | 분석 프리뷰의 별도 `분석 중` 텍스트가 진행 CTA live region과 함께 의미 트리에 노출돼 같은 비동기 상태를 중복 안내할 수 있음 | 프리뷰 overlay는 Semantics에서 제외한 24px 진행 표시만 사용하고 패널에는 `상품을 찾고 있어요`, CTA에는 구체적인 단일 live region을 제공 |
| 49 | 출처 칩을 시각 문구로만 바꾸면 보조기기가 화면 맥락 없이 `선택한 이미지`만 읽을 수 있음 | 단일 Semantics 노드에 `입력 미리보기, 선택한 이미지`처럼 역할과 값을 결합하고 카메라·이미지 경로를 화면 테스트로 검증 |
| 50 | Brand mark·상태 배지·프리뷰 칩과 진행 표시가 `32·36·20·24·28px`를 각 컴포넌트에 직접 보유해 같은 역할의 크기가 화면별로 다시 갈라질 수 있음 | `AppDesignTokens`에 `compactVisualSize`, `previewLabelMinHeight`, `inline/preview/pageProgressSize`를 추가하고 공통 컴포넌트·Scan 화면에 연결 |
| 50 | 상태 배지와 프리뷰 칩이 고정 높이를 사용하면 큰 글자에서 텍스트가 컨테이너에 갇히거나 세로로 잘릴 수 있음 | 고정 `height`를 토큰 기반 `minHeight`로 바꾸고 프리뷰 칩에 4px 수직 여백을 제공해 콘텐츠 크기에 따라 확장 |
| 50 | 프리뷰 출처가 13px, 카메라 안내가 15px, detection label이 11px 직접 스타일을 사용해 정의된 `12/14/15px` 타이포그래피 체계를 우회 | 출처는 Theme caption, 카메라 안내는 CTA 역할, detection label은 최소 12px caption 토큰에 연결하고 100%·150%·밀집 프리뷰 골든으로 재비평 |
| 50 | 토큰 선언만으로 컴포넌트가 실제 크기를 소비하는지 증명되지 않음 | 모든 신규 크기의 4px 리듬, 상태 배지 최소 높이, 페이지 진행 표시 28px, 프리뷰 칩 최소 36px를 렌더 좌표로 검증 |
| 51 | Brand Orange `#EE7203`을 흰색의 2px 포커스 경계로 사용하면 대비가 2.98:1이라 비텍스트 3:1 기준에 근소하게 미달 | 브랜드 원색과 선택선은 유지하고 포커스 전용 Deep Orange `#D96500`을 `focusRing`에 배정해 Surface·Workspace·Brand Soft에서 3:1 이상 확보 |
| 51 | 선택과 포커스가 같은 Orange를 사용하면 두 상태가 함께 있을 때 경계만으로 현재 키보드 위치를 구분하기 어려움 | Selected는 Brand `#EE7203`, Focused는 더 깊은 `#D96500`으로 역할을 분리하되 면색·아이콘·Semantics 신호는 그대로 유지 |
| 51 | 비활성 CTA 문구 `#9A9997`이 `#E8E8E6` 면에서 2.32:1이라 남은 상품 수 같은 실행 조건을 읽기 어려움 | `disabledContent`를 `#696767`로 조정해 4.5:1 이상을 확보하고 disabled surface·lock 아이콘·tap 없음으로 실행 불가 상태를 계속 구분 |
| 51 | 팔레트 값만 검토하면 실제 Surface 조합의 회귀를 잡지 못함 | Muted 일반 텍스트 4.5:1, Subtle 비텍스트·Focus Ring 3:1, disabled 문구 4.5:1을 실제 컴포넌트 표면 조합으로 자동 검증 |
| 52 | 최종 확정의 로컬 저장이 실패하면 확인 결과는 유지되지만 고정 CTA가 사라져 사용자가 동일 작업을 완료할 방법이 없음 | 오류 문구와 함께 같은 위치의 단일 Primary를 `다시 저장`으로 전환하고 보존된 응답·원본 입력·확정 결과로 `submit()`을 재실행 |
| 52 | 저장 오류 안내가 최소 결과 패널 폭에서 한 줄 말줄임되어 무엇이 보존됐는지 확인할 수 없음 | 문구를 `저장하지 못했어요. 확인 결과는 유지됐어요.`로 압축하고 오류에만 최대 두 줄을 허용해 1280×720에서 전체 정보와 CTA를 동시 노출 |
| 52 | 저장 실패 복구가 시각 문구만 바뀌면 보조기기가 실패 전환을 즉시 알지 못하고 재시도 성공 시 실제 세션 보존 여부도 증명되지 않음 | 오류 문구를 단일 live region으로 제공하고 `실패 → reviewing·확정 유지 → 다시 저장 → ready·완료 토스트` 전체 경로를 단위·위젯·골든 테스트로 검증 |
| 53 | 분석이 끝나 `ERROR` 또는 `RECAPTURE`로 전환돼도 중앙 상태 제목·설명이 일반 텍스트라 보조기기는 실패 원인과 교정 방법을 자동으로 알지 못함 | 공통 `AppEmptyState.announcement`를 추가하고 Scan의 Worker 오류·입력 오류·재촬영 판정·카메라 문제를 `제목. 해결 설명` 단일 live region으로 연결 |
| 53 | 전체 상태를 하나의 Semantics로 합치면서 복구 버튼까지 제외하면 오류는 읽히지만 키보드·보조기기 사용자가 다음 행동을 실행할 수 없음 | 상태 메시지 묶음만 `excludeSemantics`하고 `다시 분석·다른 이미지 선택·다시 연결·새로고침`은 별도 버튼·tap 노드로 유지해 메시지와 행동 책임을 분리 |
| 53 | Activity 최초 로드가 빈 결과 또는 오류로 끝나면 로딩 알림 뒤 새 상태가 자동 안내되지 않아 화면 탐색 전까지 완료 여부를 알기 어려움 | 빈 기록·필터 결과 없음·최초 로드 오류도 같은 상태 메시지 계약으로 연결하고 기존 로딩·인라인 알림과 중복되지 않게 상태별 단일 live region을 검증 |
| 54 | 검색어까지 지우는 결과 없음 행동이 `필터 초기화`로 표시돼 실행 결과가 문구보다 넓고, 상단 `모두 초기화`와 중앙 행동이 중복 노출 | 검색·입력원·기간·정렬을 모두 되돌리는 실제 결과에 맞춰 `AppActionCopy.resetAll`의 `모두 초기화`로 통일하고 결과 없음일 때 상단 중복 행동을 숨김 |
| 54 | 조건에 맞는 기록이 없을 때 유일한 완료 경로가 Outlined Secondary라 넓은 빈 화면에서 다음 행동보다 검색 실패 자체가 더 강하게 남음 | 검색창·필터는 수정 가능하게 유지하면서 중앙 `모두 초기화`를 48px 단일 Filled Primary로 격상해 토스플레이스식 빈 상태 CTA 위계 적용 |
| 54 | 주요 크기에서만 결과 없음 상태를 보면 최소 작업대에서 7개 필터·검색·정렬과 중앙 CTA가 함께 유지되는지 증명되지 않음 | `1280×720` 실제 골든에서 상단 중복 없음·단일 Primary·전체 문구·live region·tap·초기화 후 2건 복원을 통합 검증 |
| 55 | 기존 Activity 새로고침 실패 시 툴바 아이콘과 인라인 알림의 `새로고침`이 동시에 노출돼 같은 복구 행동이 서로 경쟁 | 실패가 유지되는 동안 툴바 아이콘을 숨기고 오류 문맥의 44px `새로고침` 하나만 남기며 `F5`는 유지 |
| 55 | 인라인 재시도를 누른 뒤 오류 알림과 진행 상태가 동시에 보이면 실패와 복구 중 상태가 혼동될 수 있음 | 재시도 시작 즉시 알림을 제거하고 기존 툴바 위치의 진행 아이콘·단일 live region으로 전환한 뒤 성공 시 정상 툴바를 복원 |
| 55 | 토스플레이스 선택 템플릿의 상태 표현을 문서 문구만으로 추정하면 왼쪽 띠나 불필요한 선택 장식을 다시 도입할 위험 | 공식 선택·결과·검색 템플릿 이미지를 재검토해 체크 아이콘·표면 변화·고정 CTA·행동 비중복 원칙을 현재 선택 표면 및 오류 복구 계약에 반영 |
| 56 | 객체·Top-3·검색·Activity 행이 시각적으로는 한 번에 하나만 선택되지만 공통 Semantics에는 선택 항목 사이의 상호 배타 관계가 없음 | `AppSelectableSurface.inMutuallyExclusiveGroup`을 추가하고 세 화면 목록과 후보 선택에 적용해 단일 선택 관계를 보조기기에도 전달 |
| 56 | 상품 선택 Semantics 레이블이 `상품명 … 선택`으로 행동을 반복해 button·tap·selected 발화와 책임이 겹침 | 레이블은 상품명·신뢰도만 유지하고 선택 상태·그룹 관계·실행 가능 여부를 표준 Semantics 속성으로 분리 |
| 56 | 공통 상태 샘플만으로는 실제 검수 화면에서 왼쪽 띠 없이 현재 객체와 선택 상품이 동시에 구분되는지 증명하기 어려움 | `1440×900` 실제 선택 후보 골든에서 전체 Orange 면·사방 외곽선·radio·객체 화살표·단일 최종 CTA와 overflow 부재를 함께 검증 |
| 57 | 이미지 분석 중에는 교체를 실행할 수 없는데도 회색 `다른 이미지 선택` Secondary가 남아 진행 CTA와 시각적으로 경쟁 | 분석 전용 Action bar 분기를 추가해 오른쪽 진행 `분석 중` 하나만 유지하고 실행 불가 Secondary·tooltip을 제거 |
| 57 | 모든 비동기 상태에서 Secondary를 일괄 숨기면 카메라 연결 확인이 길어질 때 사용할 수 있는 이미지 입력 경로까지 막힘 | 실제 실행 가능성을 기준으로 분리해 연결 확인 중 `이미지 선택`은 유지하고 분석 중 입력 교체만 숨김 |
| 57 | 분석 진행의 단일 행동 원칙이 위젯 문구만으로 검증되면 disabled 컨트롤이 다시 나타나도 회귀를 놓칠 수 있음 | `1280×720` 골든과 위젯 테스트에서 진행 CTA 1개·live region·tap 없음·Outlined 입력 행동·tooltip 부재를 함께 검증 |
| 58 | 분석 중 이미지 교체 버튼은 숨겼지만 `Ctrl+O`가 계속 이벤트를 소비해 화면에는 없는 행동이 남음 | `canChooseImage`를 컨트롤러의 단일 가용성 계약으로 추가하고 Action bar·명령 진입점·키보드 처리에 함께 적용 |
| 58 | 저장 중 사용자 수정이 있으면 숨은 `Ctrl+O`가 이미지 선택보다 먼저 폐기 확인 대화상자를 열 수 있음 | 분석·저장 중 키 입력을 확인 대화상자 전에 차단하고 이벤트를 처리한 것으로 가장하지 않아 숨은 오버레이와 입력 교체를 모두 방지 |
| 58 | 비동기 상태를 일괄 차단하면 카메라 연결 확인 중 유지하기로 한 대체 입력 경로가 단축키에서 사라질 수 있음 | `cameraInitializing`과 분석·저장을 분리해 연결 확인 중 화면 버튼·`Ctrl+O`는 유지하고 두 진행 상태에서만 파일 선택 호출 0회를 회귀 검증 |
| 59 | `IndexedStack`의 Activity가 자체 `Ctrl+O` 처리기를 가져 스캔의 분석·저장 상태와 무관하게 키를 먼저 소비 | `canChooseImageShortcut`을 Activity 입력 계약으로 전달해 현재 화면과 무관하게 컨트롤러의 단일 가용성 판정을 공유 |
| 59 | Activity에서 차단된 단축키가 콜백 내부에서만 조용히 반환되면 운영자와 OS에는 실행된 키처럼 보임 | Activity 처리기와 루트 처리기 모두 실행 불가 상태에서 `ignored`를 반환하고 파일 선택 호출·화면 전환을 만들지 않음 |
| 59 | 준비 상태의 전역 이미지 선택까지 함께 막히면 Activity에서 스캔으로 빠르게 복귀하는 기존 운영 흐름이 깨짐 | 같은 위젯 테스트에서 분석 중 `handled=false·pick=0·Activity 유지` 후 준비 상태 `handled=true·pick=1·스캔 이동`을 연속 검증 |
| 60 | 색상·간격·반경은 토큰화됐지만 확인 대화상자 `400px`, 빈 상태 `360px`, 진단 레이블 `88px`가 컴포넌트와 화면에 원시값으로 남음 | `dialogWidth`, `emptyStateMaxWidth`, `metadataLabelWidth`를 `AppDesignTokens`에 추가하고 공통 컴포넌트와 Activity 상세가 직접 사용 |
| 60 | 포커스 `2px`와 선택 `1.5px`가 Theme 버튼·입력·필터·드롭다운·선택 표면마다 반복돼 상태 강도가 화면별로 갈라질 수 있음 | `focusRingWidth`, `selectionOutlineWidth`를 추가해 모든 상호작용 표면이 같은 두께 계약을 공유 |
| 60 | 모든 숫자를 토큰으로 승격하면 glyph별 광학 보정과 1px 구분선까지 의미 없는 API가 되어 시스템이 오히려 불명확해짐 | 반복되는 운영 레이아웃·상태 치수만 승격하고 아이콘 광학 크기·모델 좌표·기본 1px 구분선은 명시적 예외로 유지하며 원시 역할 검색 0건 검증 |
| 61 | 화면과 공통 컴포넌트에 `FontWeight.w500/w600/w700`이 24곳 남아 같은 Pretendard 위계를 숫자로 반복 | 모든 굵기를 `AppTypography.regular/medium/semibold/bold` 역할로 교체하고 확인 필요 Bold·확정 confidence Medium 등 기존 의미 대비를 보존 |
| 61 | Activity 필터의 13px 글자가 caption의 배율 행간을 암묵적으로 상속해 실제 행간이 토큰 표와 분리 | `compactControlSize 13`과 `compactControlLineHeight 20`을 정의해 44px 필터 안에서 명시적인 13/20 리듬으로 수직 정렬 |
| 61 | 앱명·목록 레이블의 tracking과 Theme 제목 tracking이 화면별 숫자로 남아 글꼴 조정 시 한 번에 변경할 수 없음 | `titleTracking`, `sectionTracking`, `brandTracking`, `dataLabelTracking`으로 통합하고 lib의 직접 `FontWeight/fontSize/letterSpacing` 검색을 토큰 선언부 외 0건으로 고정 |
| 61 | 컴팩트 행간 명시로 Activity 골든에 약 0.11% 차이가 발생 | 대표 `1440×900` 목록을 직접 비평해 필터 레이블에만 국한되고 조작 영역·선택선·레이아웃이 유지됨을 확인한 뒤 관련 골든을 승인 |
| 62 | 같은 `easeOutCubic` 곡선이 공통 컴포넌트와 화면 8곳에 직접 반복되어 모션 성격을 한 번에 조정할 수 없음 | `AppMotion.interactionCurve`로 통합하고 모든 상태·패널 전환이 같은 토큰을 사용하도록 정적 검색으로 고정 |
| 62 | 시스템에서 애니메이션을 줄여도 5곳의 불확정 진행 표시가 계속 회전해 reduced motion 계약과 충돌 | 공통 `AppProgressVisual`을 도입해 일반 환경은 기존 회전 표시, reduced motion은 동일 크기의 정적 대기 아이콘으로 전환 |
| 62 | 진행 아이콘을 정적으로 바꾸면서 상태 전달까지 사라지거나 보조기기가 아이콘과 문구를 중복 발화할 위험 | 진행 시각은 `ExcludeSemantics`로 장식 처리하고 기존 상태 문구·단일 live region을 그대로 유지하는 위젯 테스트 추가 |
| 63 | 오류·재촬영·진행·저장 완료는 자동 안내되지만 정상 `APPROVED` 결과와 마지막 수동 확인 완료는 조용히 바뀌어 보조기기 사용자가 최종 확정 가능 여부를 놓칠 수 있음 | 화면을 차지하지 않는 공통 `AppStateAnnouncement`를 추가해 모든 검수가 끝난 순간 `n개 상품 확인 완료. 최종 확정할 수 있어요.`를 단일 live region으로 전달 |
| 63 | 모든 reviewing 상태에 요약 live region을 추가하면 기존 `n번 상품을 확인해 주세요` 현재 항목 안내와 동시에 발화해 오히려 다음 행동이 흐려짐 | 미완료 검수에는 기존 현재 항목 안내 하나만 유지하고 `allConfirmed`일 때만 완료 알림을 마운트하도록 범위를 축소 |
| 63 | 시각 사용자에게 이미 충분한 헤더·고정 CTA가 있는데 접근성 알림 때문에 빈 여백이나 새 포커스 정지점이 생길 위험 | `SizedBox.shrink` 기반 0px·tap 없음·live region 전용 계약을 공통 컴포넌트 테스트와 APPROVED/UNKNOWN 흐름 테스트로 고정 |
| 64 | 운영 `bread_ko.json`에 한국어 `display_name_ko`가 모두 있지만 로더가 이를 무시하고 영문 `class_name`을 화면명으로 복사해 실제 앱·골든이 `Muffin/Scon/Bagel`을 노출 | 카탈로그 파서의 표시명을 `display_name_ko`로 연결하고 `localize`·후보·검색 결과·신규 로그가 `머핀/스콘/베이글`을 사용하도록 복구 |
| 64 | 한국어 화면명으로 바꾸며 모델 영문 원본명 또는 class ID 검색이 사라지면 현장 예외 검색과 진단 흐름이 약화 | `displayName`·`className`·`classId` 세 필드를 모두 검색 대상으로 유지하고 `머핀`, `Muffin`, `bread_13`이 같은 한국어 결과를 반환하는 테스트 추가 |
| 64 | 테스트 응답을 화면에 직접 주입하면 실제 Controller 현지화 경로를 건너뛰어 영어 골든이 통과하면서 운영 결함을 숨길 수 있음 | 공통 `testReviewDetections`로 자동 승인 결과까지 운영 카탈로그와 동일하게 현지화하고 52개 골든을 한국어 실데이터로 다시 렌더링·비평 |
| 65 | 후보를 Enter로 확정하면 다음 미확정 객체는 선택되지만 기존 후보 행이 사라져 포커스가 작업 흐름에서 이탈 | 키보드 확정 시 다음 객체의 첫 후보, 마지막 확정 시 고정 `최종 확정` CTA로 포커스를 이어가는 `_ResultPanel` 수준 인계 추가 |
| 65 | Material의 `ActivateIntent` 실행 시점에는 pressed-key 집합이 비어 키보드와 pointer 입력을 `HardwareKeyboard`로 안정적으로 구분할 수 없음 | `AppSelectableSurface`가 pointer `onTapDown/onTapCancel`을 추적하고 pointer 이벤트가 없는 `onTap`만 `onKeyboardTap`으로 분류하도록 공통 계약화 |
| 65 | 이미 포커스된 외부 FocusNode를 새 후보 행에 다시 연결하면 실제 primary focus는 맞지만 새 표면의 Focus Ring·Semantics `focused`가 false로 시작 | 선택 표면의 `initState/didUpdateWidget`에서 외부 노드의 현재 상태를 동기화하고 후보 3개 연속 확정·검색 결과 확정·마우스 비인계 테스트로 고정 |
| 66 | 64차 이후 신규 판정은 한국어로 저장되지만, 이전 앱이 이미 저장한 Activity 로그의 `final_product.class_name`은 영문이라 같은 목록에서 표시 언어가 혼합 | 저장 JSON과 `ScanLogRepository.list(limit: 100)` 계약은 유지하고, 로드된 요약의 기존 `class_id`를 현재 카탈로그에 연결해 화면 메모리에서만 한국어 표시명으로 정규화 |
| 66 | 표시명만 한국어로 치환하면 작업자가 익숙한 기존 영문 상품명이나 기술 식별자로 Activity를 다시 찾지 못할 수 있음 | Activity 요약에 저장되어 있던 원본 `class_name`·`class_id`를 보존하고 검색은 한국어 표시명·영문 원본명·class ID를 모두 AND 필터의 검색 조건으로 유지 |
| 66 | 카탈로그에서 제거되거나 식별자가 바뀐 과거 상품을 강제로 현재 상품에 매핑하면 감사 기록의 의미가 변형될 수 있음 | `class_id → class_name → 기존 표시명` 순서로 정확히 일치할 때만 현지화하고 일치 항목이 없으면 저장된 상품명을 그대로 표시 |
| 67 | Flutter 토큰과 골든은 최소 `1280×720`을 가정하지만 Windows 창은 그보다 작게 축소할 수 있어 검증되지 않은 적층 레이아웃에 진입 | Win32 `WM_GETMINMAXINFO`에 최소 Flutter client area를 연결해 창 드래그 단계부터 `1280×720` 논리 작업대를 보장 |
| 67 | 최소 크기를 물리 픽셀 `1280×720`로 고정하면 Windows 125%·150% 배율에서 Flutter 논리 작업대가 각각 1024×576·853×480으로 축소 | `FlutterDesktopGetDpiForHWND`와 DPI별 window frame 계산으로 현재 모니터 배율을 반영하고, 모니터 이동 뒤에도 논리 최소 크기를 다시 계산 |
| 67 | Dart `AppBreakpoints`와 C++ 런너 값이 서로 다른 파일에 있어 한쪽만 변경되면 지원 계약이 조용히 분리 | 소스 계약 테스트로 네이티브 `1280·720`, `SetMinimumSize`, DPI 처리 메시지가 Flutter 토큰과 함께 유지되는지 검증하고 Windows Release 빌드로 컴파일 확인 |
| 68 | `final_product` 또는 `confirmation_method`가 없는 구버전 로그가 Activity에서 `Unknown`·`UNKNOWN`으로 노출되어 사용자가 내부 sentinel의 의미를 추측해야 함 | 저장 데이터는 변경하지 않고 Activity 표시 경계에서 `상품 정보 없음`·`확정 방식 확인 불가` 콘텐츠 토큰으로 바꿔 원인과 정보 부재를 한국어로 명시 |
| 68 | 66차에서 영문 상품명·class ID 검색은 복원했지만 다중 상품 목록 요약은 한국어 표시명만 검사해 검색과 일치하지 않은 첫 상품을 계속 표시 | 검색 판정과 요약의 일치 함수를 하나로 통합해 `베이글`·`Bagel`·`bread_11` 모두 `베이글 외 n개`를 첫 결과로 표시 |
| 68 | 필터·목록 요약·상세 상품·진단 확정 방식이 각자 fallback을 만들면 같은 결측 데이터가 화면마다 다른 이름으로 나타날 수 있음 | `activity_presentation.dart`에 상품 표시명·검색 일치·요약·확정 방식 매핑을 모으고 단위 테스트와 실제 `1280×720` Activity 통합 테스트로 연결 |
| 69 | Activity 행은 Enter·Space로 선택할 수 있지만 최대 100건을 순서대로 살피려면 각 행에 Tab을 반복해야 해 Windows 검수 흐름이 느림 | 목록 포커스 내부에서만 `↑/↓`를 처리해 선택 행·오른쪽 상세·키보드 포커스를 한 번에 이전·다음 기록으로 이동 |
| 69 | 새 행 FocusNode를 요청하면 Flutter의 기본 `showOnScreen`이 이미 보이는 2번째 행까지 목록 맨 위로 올려 앞 기록 문맥이 사라짐 | 포커스 확정 다음 프레임에 60px 행 정렬 기반 선택 노출 정책을 다시 적용해 주변 기록을 유지하고, 20건 마지막 행까지 완전 노출을 검증 |
| 69 | 목록 방향키를 Activity 루트에서 처리하면 검색창의 커서 이동이나 한글 조합 중 키 입력과 충돌할 수 있음 | 키 이벤트 범위를 `_LogList`의 비포커스 ancestor로 제한해 행에 포커스가 있을 때만 탐색하고 검색 TextField에서는 선택·상세를 바꾸지 않음 |
| 70 | 카메라 확인·오류는 live region으로 안내되지만 재연결 성공은 정적 상단 배지만 바뀌어 보조기기 사용자가 완료 여부를 알기 위해 화면을 다시 탐색해야 함 | `AppStatusBadge.liveRegion`을 선택적 계약으로 추가하고 `카메라 연결됨` 전환만 자동 안내해 다음 촬영 행동이 가능함을 즉시 전달 |
| 70 | 모든 상태 배지를 live region으로 만들면 `카메라 확인 필요` 배지와 본문의 구체적인 오류·복구 안내가 동시에 발화 | 확인 중·미연결·확인 필요 배지는 정적으로 유지하고 오류는 기존 본문 live region 하나만 사용하도록 성공과 실패의 알림 책임 분리 |
| 70 | 공통 배지 기본값이 자동 알림이면 Activity의 `저장됨`·`수정됨`처럼 화면 탐색으로 읽을 상태까지 매 rebuild마다 끼어들 수 있음 | `liveRegion: false`를 기본값으로 고정하고 카메라 성공만 명시적으로 opt-in하는 컴포넌트·초기 성공·실패·재연결 테스트 추가 |
| 71 | 최근 100건 Activity에서 `↑/↓`만으로 처음과 끝을 오가려면 최대 99회 입력이 필요해 장시간 감사 작업의 이동 비용이 큼 | Windows listbox 관례에 맞춰 `Home·End`를 현재 필터·정렬 결과의 처음·끝 기록으로 연결하고 기존 선택·상세·포커스 동기화 재사용 |
| 71 | Page Up/Down이 고정 숫자로 이동하면 `1280×720`, 큰 글자, 향후 토큰 변경에서 실제 viewport와 페이지 경계가 어긋남 | 현재 목록 viewport를 `rowHeight`로 나눠 가시 행 수를 계산하고 한 행을 문맥으로 겹친 `visibleRows - 1`만큼 이동 |
| 71 | 빠른 이동키를 전역 단축키로 등록하면 검색창의 Home·End 커서 이동과 Page Up/Down 편집 동작을 빼앗음 | 69차와 같은 `_LogList` 포커스 범위에서만 처리하고 TextField 포커스 후 Home 입력이 선택 상세를 바꾸지 않는 회귀 검증 |
| 72 | BIXOLON 대각선 모티프는 Scan 프리뷰에만 머물렀지만 private painter가 비율·코너·중앙 표식·선 굵기와 색 투명도를 원시값으로 직접 생성 | `AppScanGuide` 공통 컴포넌트와 `scanGuide*`·`previewGuide*` ThemeExtension 토큰으로 승격해 브랜드 시그니처도 테마 계약을 따르도록 통합 |
| 72 | 선택·CTA·Activity까지 대각선 장식이 번지면 토스플레이스식 조용한 표면과 BIXOLON 고유 표현의 역할이 서로 경쟁 | 결과가 없는 Scan 프리뷰에서만 가이드를 마운트하고 결과 화면에서는 bounding box로 교체하는 위젯 범위 테스트와 사용 금지 계약을 고정 |
| 72 | 광학 치수까지 4px 배수로 기계적으로 바꾸면 이미 검증된 촬영 가이드 비례가 달라지고 불필요한 골든 변동이 발생 | 기존 `64%·34·7·2.5px` 렌더링을 보존하되 광학 예외를 문서화하고 53개 골든 해시 무변경으로 시각 회귀가 없음을 검증 |
| 73 | `ERROR`와 `RECAPTURE`가 문구·아이콘·CTA로는 분리됐지만 둘 다 같은 촬영 가이드를 보여 서버 장애도 촬영 구도가 원인처럼 보임 | `ERROR`에서는 가이드를 제거하고 실제 구도 교정이 필요한 `RECAPTURE`에만 유지해 프리뷰에서도 두 도메인을 분리 |
| 73 | 카메라 확인 중·미연결·촬영 실패에는 정렬할 수 있는 라이브 소스가 없는데 대각선 가이드가 남아 장식처럼 보임 | 실제 카메라 준비 완료 또는 입력 이미지가 있을 때만 가이드를 허용하고 카메라 확인·복구 상태에서는 원인·진행·다음 행동만 노출 |
| 73 | 제출 완료 뒤 카메라 미연결 상태로 돌아갈 때 이전 가이드가 재등장하면 완료 토스트와 새 촬영 준비가 한 프리뷰에서 경쟁 | 완료 후 입력이 없는 상태도 동일한 소스 가용성 계약을 적용하고 변경된 10개 골든을 직접 비평해 정보 위계와 `RECAPTURE` 유지 여부를 승인 |
| 74 | Activity 새로고침을 키보드로 실행하면 실패 시 툴바 아이콘이 사라지고 인라인 복구 버튼이 새로 생겨 포커스가 작업 흐름에서 이탈 | 툴바와 인라인 복구 행동에 별도 FocusNode를 두고 키보드 실행 의도를 실패·재시도·성공 상태 사이에 명시적으로 인계 |
| 74 | 같은 FocusNode를 서로 다른 Material 버튼에 재부착하면 Semantics는 focused지만 새 버튼이 focus 상태 변경을 받지 못해 시각 링이 없는 불일치 발생 | 각 표면의 FocusNode를 분리하고 전환 후 `requestFocus`해 Semantics와 Deep Orange 2px 링이 함께 나타나는 전용 골든 추가 |
| 74 | 비동기 새로고침 중 사용자가 검색·필터로 이동했는데 완료 후 이전 아이콘이 포커스를 되가져오면 현재 작업을 방해 | 진행 중 Tab 또는 pointer 입력을 감지해 자동 복원을 취소하고 사용자가 선택한 FocusNode가 성공 후에도 유지되는 회귀 테스트 추가 |
| 75 | 저장 실패 테스트가 앱을 렌더링하기 전에 `submit()`을 호출해 실제 키보드 최종 확정→실패 포커스 인계가 검증되지 않음 | Tab으로 `최종 확정`에 도달하고 Enter로 저장을 시작한 통합 경로에서 동일 FocusNode의 `다시 저장` focused Semantics를 검증 |
| 75 | Orange `다시 저장` CTA가 의미상 focused여도 Ink 2px 경계가 실제 오류 바에서 보이는지는 기존 비포커스 골든으로 증명되지 않음 | `scanner_save_error_focus_1280x720` 골든을 추가해 오류 문구·단일 Primary·Orange 면·Ink 포커스 경계를 함께 시각 승인 |
| 75 | 재시도 성공 뒤 사라진 CTA에 포커스가 남거나 완료 안내가 중복되면 다음 작업 시작이 막힐 수 있음 | Enter 재시도 후 `다시 저장` 제거·단일 완료 live region·이전 FocusNode 이탈·다음 Tab 탐색 가능성을 한 테스트에서 연속 검증 |

| 76 | 이미지 작업을 저장한 뒤 입력 모드가 카메라로 초기화되어 연속 이미지 검수자가 불필요한 `다시 연결` 단계를 거쳐야 했음 | 성공 저장 시 완료한 입력원을 유지하고, 이미지 모드는 이미지 전용 빈 미리보기·`다음 이미지를 선택해 주세요` 안내·단일 `이미지 선택` Primary와 `Ctrl+O`로 다음 작업을 즉시 시작하도록 개선. 카메라 완료와 수동 세션 초기화는 기존 카메라 대기 동작을 유지 |
| 76 | 실제 입력이 없는 이미지 대기 상태에서도 카메라 미리보기와 출처 칩이 남아 현재 입력 맥락을 혼동시킬 수 있었음 | 이미지 대기 전용 placeholder를 추가하고 실제 이미지가 없을 때 출처 칩과 스캔 가이드를 숨김. 상단 상태 배지는 `이미지 입력 · 카메라 미연결`로 입력 맥락을 유지 |
| 77 | 키보드로 최종 확정·재시도를 완료하면 이전 CTA가 사라지며 포커스도 문맥을 잃어 다음 파일을 고르려면 다시 Tab 탐색해야 했음 | 완료 직전 포커스가 최종 확정 CTA에 남아 있는 경우에만 다음 `이미지 선택` Primary로 포커스를 전달. Enter와 `Ctrl+O`가 모두 다음 파일 선택으로 이어지고, 저장 중 사용자가 다른 컨트롤로 이동했다면 포커스를 강제로 되돌리지 않음 |
| 77 | 완료 화면의 1280×720 포커스·단일 Primary·빈 미리보기 조합이 시각 회귀로 증명되지 않았음 | `scanner_completed_focus_1280x720` 골든에서 Ink 2px 포커스 링, 48px CTA, 완료 토스트, 단일 Primary와 오버플로 부재를 함께 검증 |
| 78 | Activity를 이미 열었던 세션에서 새 스캔을 저장하면 완료 토스트는 보이지만 캐시된 목록에는 방금 저장한 기록이 없을 수 있었음 | 성공 저장 때만 Activity data revision을 증가시키고 Activity가 해당 revision을 아직 읽지 않았을 때 자동 갱신. 화면 전환만으로는 불필요하게 다시 로드하지 않음 |
| 78 | 최신 기록을 반영하려고 Activity를 재생성하면 검색·입력원·기간·정렬·선택 상태를 잃을 위험이 있었음 | 기존 `IndexedStack`과 Activity State는 유지한 채 목록 데이터만 갱신하고, 기존 검색·필터·정렬·유효한 선택을 보존. `activity_new_save_synced_1280x720` 골든에서 완료 토스트·유지된 검색어·새 기록·상세를 함께 검증 |
| 79 | 자동 동기화로 새 기록이 목록에 추가되어도 과거 선택이 계속 존재하면 상세 패널은 이전 기록을 유지해 저장 직후 확인 의도와 어긋남 | 성공 저장의 Scan ID를 별도로 전달하고 revision 기반 자동 동기화에서만 해당 기록을 선택. 수동 `F5` 새로고침은 기존 선택을 유지 |
| 79 | 활성 검색 조건에서 새 기록이 임시로 선택되더라도 검색을 지우면 과거 선택으로 되돌아갈 수 있었음 | 최신 저장 Scan ID를 Activity 선택 상태 자체에 반영해 검색 해제 후에도 새 행과 상세가 선택됨을 Semantics `selected`와 상세 key로 검증. 검색·필터 조건은 자동으로 변경하지 않음 |
| 80 | 오래된순에서 최신 선택이 긴 목록 끝으로 이동할 때 화면 밖에 남을 가능성을 새 동기화 흐름에서 재검토할 필요가 있었음 | 기존 `_LogList`가 선택 ID뿐 아니라 정렬 후 index 변화도 감지해 60px 행 경계로 자동 스크롤하고, 오래된순 20건 골든과 좌표 검증이 같은 위험을 이미 방지함을 확인해 중복 구현하지 않음 |
| 81 | 단계 탐색 중앙 폭 `52px`, 프리뷰 입력원 마커 `6px`, 상단 탐색 현재 위치 두께 `3px`가 컴포넌트에 직접 남아 구조 토큰 감사에서 누락 | `stepNavigatorLabelWidth`, `previewSourceMarkerSize`, `navigationIndicatorThickness`로 승격하고 공통·화면 컴포넌트가 ThemeExtension 값을 사용하도록 연결 |
| 82 | 저장 완료 토스트와 저장으로 촉발된 Activity 자동 동기화가 같은 순간 각각 live region으로 발표되어 보조기기 사용자가 완료와 백그라운드 진행을 연속해서 들어야 했음 | 자동 동기화는 진행 아이콘·disabled 상태·접근 가능한 `새로고침 중` 레이블을 유지하되 live 발표만 끄고, 완료 토스트를 단일 우선 알림으로 유지 |
| 82 | 자동 동기화 알림을 전역으로 끄면 사용자가 직접 실행한 `F5`·새로고침의 진행 피드백까지 사라질 위험 | `AppIconActionButton.announceProgress`를 기본 true로 추가하고 stale-only 갱신에만 false를 전달. 수동 새로고침은 기존 live region·포커스 복원 계약을 그대로 유지 |
| 83 | 저장은 성공했지만 Activity 자동 동기화가 실패하면 기존 일반 새로고침 오류만 보여 데이터 자체가 유실됐는지 판단하기 어려움 | 저장 성공 사실과 화면 갱신 실패를 한 문장으로 분리하고, 기존 목록이 있으면 비차단 inline 안내, 없으면 전용 empty 안내와 단일 `새로고침` 복구 행동을 제공 |
| 83 | 저장 성공 뒤의 표시 장애를 Error Red로 강조하면 저장 실패로 오해할 수 있고, 재시도에서 최신 저장 기록 선택 의도를 잃을 수 있음 | 자동 동기화 실패에만 Attention Amber를 적용하고 최신 저장 Scan ID를 재시도까지 보존. 성공 시 해당 행·상세를 선택하며 초기 로드·수동 새로고침 실패의 Error 계약은 유지 |
| 83 | 성공 토스트와 동기화 실패 안내가 동시에 보이는 실제 최소 작업대에서 정보 위계와 기존 목록 보존이 검증되지 않음 | `activity_saved_sync_error_1280x720` 골든에서 상단 완료 토스트, 주의 안내, 단일 복구 행동, 기존 목록·상세 유지와 오버플로 부재를 함께 시각 승인 |
| 84 | 저장 직후 오류 골든을 Attention 계약으로 바꾸면서 저장 맥락이 없는 일반 최초 로드 실패의 Error 계약을 독립적으로 증명하는 화면 회귀가 사라짐 | 연속 두 번의 초기 로드 실패를 재현하고 일반 제목·보존 설명·Error Red 아이콘·단일 `새로고침`·live region을 별도 테스트로 고정 |
| 84 | 저장 동기화 실패와 일반 로드 실패가 같은 중앙 empty 레이아웃을 사용해 문구만 회귀해도 육안으로 놓칠 위험 | `activity_initial_error_1280x720` 골든을 추가해 일반 오류의 빨간 상태 표식과 저장 맥락 문구 부재를 Attention 골든과 교차 승인 |
| 85 | `IndexedStack`이 앱 시작부터 비활성 Activity를 생성해 사용자가 활동을 열지 않아도 저장소의 모든 JSON 파일을 읽고 파싱하며 카메라 준비와 I/O·UI isolate 시간을 경쟁 | Activity는 첫 방문에서만 생성하고 이후에는 같은 State를 유지해 검색·필터·정렬·선택 보존과 시작 비용 제거를 함께 만족 |
| 85 | 지연 생성된 Activity가 처음부터 active이면 기존 `inactive → active` 훅이 없어 `/`·`F5` 단축키 범위 포커스가 설정되지 않음 | 생성 직후에도 동일한 post-frame shortcut focus를 요청하고 첫 방문 F5·재방문 무재로드를 위젯 테스트로 고정 |
| 85 | 저장 후 첫 Activity 방문의 중앙 `AppLoadingState`가 항상 live region이면 완료 토스트와 로딩 발표가 다시 경쟁 | `AppLoadingState.announce`를 기본 true로 추가하고 최신 저장 Scan ID가 있는 최초 동기화에만 false를 전달. 완료 토스트는 live, 로딩 레이블은 접근 가능하지만 non-live이며 성공 뒤 최신 행·상세를 선택 |
| 86 | Activity 최초 로드가 오래 걸릴 때 스캔 화면으로 이동하면 요청을 취소·중복하거나, 완료 뒤 재진입해도 단축키 범위가 복원되지 않을 위험 | 진행 중 요청은 한 번만 유지하고 비활성 상태에서 결과를 캐시한 뒤 재진입 시 무재로드로 표시. 생성·재활성화 양쪽에서 shortcut focus를 복원해 `F5` 요청까지 검증 |
| 86 | 최초 로드가 비활성 중 실패하면 재진입 첫 프레임에 과거 오류가 보이고 직후 자동 로딩으로 바뀌어 오류·진행 live region이 연속 발표될 수 있음 | stale revision 재진입은 `didUpdateWidget`에서 즉시 새 요청 상태로 전환해 오류 프레임을 렌더링하지 않고, 진행 중인 기존 요청에는 두 번째 요청을 만들지 않음 |
| 87 | 사용자가 저장 중 Activity를 열어 최초 로드가 진행되는 사이 저장이 완료되면 기존 로딩 live region과 완료 토스트가 동시에 활성화될 수 있음 | `dataRevision` 증가와 최신 저장 Scan ID를 감지하는 즉시 현재 로딩의 자동 발표만 끄고, 완료 토스트를 단일 우선 live region으로 유지 |
| 87 | 저장 완료 시 진행 중 최초 로드를 취소하거나 병렬 재요청하면 디스크 I/O가 중복되고 어느 응답이 최신 선택을 결정하는지 불명확 | 현재 요청은 그대로 한 번 완료하고 requested revision이 뒤처졌을 때만 후속 동기화를 한 번 실행. 최신 Scan ID 행·상세 선택과 총 2회 요청을 경쟁 상태 테스트로 검증 |
| 88 | 최신 공식 Front 단일·다중 선택·검색·결과 이미지를 다시 대조하면 선택 불가 옵션은 표면과 글자가 약해지고 탭 행동도 제거되지만, Scanner 저장 중 객체 행은 Controller만 입력을 무시해 활성처럼 보였음 | `AppSelectableSurface.enabled`와 `disabledContentOpacity 0.56` 토큰을 추가해 disabled 배경·콘텐츠·cursor·focus·tap·Semantics를 하나의 상태 계약으로 통합 |
| 88 | 저장 snapshot 생성 뒤 객체 행·프리뷰 box·단계 화살표가 계속 활성처럼 보이면 클릭 무반응 또는 저장 내용과 화면 선택 불일치를 예상하게 됨 | 저장 중 검수 상세 선택지는 숨기고 객체 행·프리뷰 box·단계 이동을 명시적으로 disabled 처리. pointer·전역 방향키 입력이 선택/상품을 바꾸지 않는 테스트와 `scanner_submitting_review_locked_1280x720` 골든으로 승인 |
| 89 | 실제 Windows 릴리스 창의 제목 표시줄·작업 표시줄에 Flutter 기본 파란 아이콘이 남아 앱 내부 Orange 포커스 마크와 제품 정체성이 분리됨 | 공식 로고를 재제작하지 않고 앱의 Orange 포커스 심볼을 16–256px 9단계 ICO로 생성해 네이티브 리소스에 적용. 표준 라이브러리 전용 생성기와 `--check`를 추가해 Brand Orange·Ink·프레임 구성을 재현 가능하게 고정 |
| 90 | 준비·분석·승인·확인 필요·재촬영·오류·저장·완료와 Activity 목록·상세·빈·오류 상태의 골든이 한쪽 해상도에 집중되어 `1280×720`과 `1440×900` 모두에서의 완료 조건을 직접 증명하지 못함 | 두 해상도 × 11개 운영 상태의 22개 레이아웃 매트릭스를 추가해 Scaffold 크기, 예측 가능한 상태 문구, RenderFlex 예외 없음, Primary 최대 1개, 모든 Primary 48px 이상을 공통 계약으로 검증 |
| 91 | 개별 컴포넌트 크기 테스트만으로는 화면 조합 과정에서 병합된 Semantics 또는 제약으로 상단 탐색·필터·목록·Bounding Box의 실제 클릭 영역이 44px 아래로 줄어드는 회귀를 증명하지 못함 | 90차의 두 해상도·22개 상태에서 활성 `SemanticsAction.tap` 노드 전체를 재귀 수집하고 실제 rect의 폭과 높이가 모두 44px 이상인지 검사. 비활성 진행·저장 노드는 tap action이 없어 검사 대상과 실행 가능 상태가 일치 |
| 92 | 조작 영역이 44px 이상이어도 접근 가능한 이름이 비어 있으면 보조기기에는 대상이 없는 `버튼`으로만 전달되어 현장 작업자가 행동 결과를 예측할 수 없음 | 같은 두 해상도·22개 운영 상태에서 모든 활성 tap 노드의 `label` 또는 `hint`가 비어 있지 않은지 함께 검사해 시각 크기와 접근 가능한 이름을 하나의 조작 계약으로 고정 |
| 93 | 결과 프리뷰의 Bounding Box는 마우스 tap과 Semantics action을 제공하지만 키보드 포커스 대상이 아니어서 화면에 보이는 객체 선택 경로가 입력 방식에 따라 달랐음 | 각 box를 focusable `InkWell`로 전환하고 Semantics focused 상태, Enter·Space 실행, Deep Orange 2px 포커스 링을 연결. 선택 Orange 면·상태 Green/Attention은 유지해 선택·상태·포커스 역할을 분리 |
| 93 | 44px rect 자체가 충분해도 화면 경계 밖으로 일부 잘리거나 포커스를 받을 수 없는 tap 노드는 최소 조작 영역 검사만 통과할 수 있음 | 두 해상도·22개 운영 상태의 모든 활성 tap rect가 Scaffold 안에 완전히 포함되고 `isFocused != none`인 키보드 대상인지 함께 검사해 마우스 전용·잘린 행동을 차단 |
| 94 | Preview Box에 포커스를 둔 뒤 전역 `↑/↓`로 객체를 바꾸면 Orange `현재 검수`는 새 Box로 이동하지만 Deep Orange 포커스 링은 이전 Box에 남아 두 개의 현재 위치가 생김 | Preview 내부 FocusNode를 객체 ID별로 관리하고, 방향키 입력 당시 포커스가 Preview Box 안에 있었을 때만 새 선택 Box로 포커스를 인계. 후보·목록 등 다른 영역의 포커스는 빼앗지 않음 |
| 94 | 장기 실행 중 Scan ID마다 객체 ID가 달라지면 Preview FocusNode 맵이 완료된 스캔 수만큼 누적될 수 있음 | 현재 detection 집합에서 사라진 노드는 해당 frame의 Box 제거가 끝난 뒤 dispose하고 세션 초기화까지 무예외로 검증 |
| 95 | 모든 Preview Bounding Box를 일반 Tab 순서에 넣으면 밀집된 6개 객체만으로도 결과 패널과 고정 CTA에 도달하기 전 불필요한 정지점이 반복되고, 객체 수에 따라 키보드 작업 비용이 증가 | 현재 선택 Box만 `skipTraversal=false`로 두는 roving focus를 적용. 비선택 Box의 직접 focus·tap·Semantics는 유지하고 Enter·Space·`↑/↓` 선택 때 순차 탐색 대상도 새 현재 객체로 이동 |
| 96 | Activity의 입력원 3개·기간 4개 필터가 각각 Tab 정류장이어서 검색부터 정렬까지 이동할 때 같은 선택 그룹을 7번 반복 탐색 | 그룹마다 현재 선택 칩 하나만 Tab 순서에 두는 roving focus를 적용. Tab은 입력원·기간 그룹을 한 번씩 통과하고 `←/→/↑/↓`는 그룹 안에서 순환하며 선택·포커스·필터 결과를 함께 갱신 |
| 96 | 방향키로 값만 바뀌고 포커스 링 또는 보조기기 `selected` 상태가 이전 칩에 남으면 현재 필터를 두 곳에서 다르게 전달 | 공통 `AppFilterGroup`이 값별 FocusNode와 `skipTraversal`을 관리하고 실제 `1440×900` Activity 골든에서 카메라 선택 면·Deep Orange 포커스 링·1건 결과를 한 화면으로 승인 |
| 97 | 최근 100건 Activity는 목록 방향키를 지원해도 모든 기록 행이 Tab 정류장으로 남아 상세 진단에 도달하려면 최대 100번을 반복해야 함 | 현재 선택 행 하나만 `skipTraversal=false`로 유지하는 roving focus를 적용. `↑/↓`·`Home/End`·`Page Up/Page Down`으로 선택·상세·포커스와 Tab 정류장을 함께 이동 |
| 97 | 스크롤된 마지막 행에서 Tab을 누르면 Flutter의 공간 기반 순서가 오른쪽 상세로 이어지지 않고 포커스가 사라질 수 있음 | 넓은 Activity 작업대에서 목록의 forward Tab을 상세 `진단 정보` disclosure로 명시적으로 인계. `1280×720` 골든으로 선택 행의 Brand 외곽선과 상세의 Deep Orange 포커스 링이 서로 다른 위치·역할로 동시에 구분됨을 승인 |
| 98 | 목록→상세 forward Tab을 고정한 뒤 `진단 정보`에서 Shift+Tab하면 공간 기반 순서가 선택 행을 건너뛰고 상단 정렬로 이동 | Activity가 목록 상태와 상세 disclosure FocusNode를 연결해 `선택 기록 ⇄ 진단 정보`의 Tab·Shift+Tab 인계를 대칭화. 스크롤된 마지막 기록에서도 같은 행으로 정확히 복귀 |
| 98 | 상세 기록 변경 때마다 포커스 노드를 새로 만들면 키보드 왕복 중 포커스가 분리되거나 장기 세션에서 노드가 누적될 위험 | 화면 생명주기의 단일 상세 FocusNode와 목록 GlobalKey를 유지하고 기존 행별 FocusNode 정리 정책을 재사용해 필터·정렬·스크롤 뒤에도 현재 선택 정류장만 복원 |
| 99 | 다중 스캔의 객체 목록은 방향키 선택을 지원해도 모든 행이 Tab 정류장이며, 행 포커스에서 `↑/↓`를 누르면 Orange 선택은 이동하지만 Deep Orange 포커스는 이전 행에 남음 | 객체별 FocusNode를 목록 상태가 관리하고 현재 선택 행만 `skipTraversal=false`로 유지. 행 포커스 안의 `↑/↓`는 선택·검수 상세·스크롤·포커스와 다음 Tab 정류장을 함께 이동 |
| 99 | 객체 행에서 Tab을 누르면 공간 기반 순서가 아래 Top-3 대신 왼쪽 Preview Bounding Box로 이동해 검수 흐름이 역행 | 확인 필요 객체에서는 `현재 객체 → 첫 후보` forward Tab과 `첫 후보 → 같은 객체` Shift+Tab을 명시적으로 연결. `1280×720` 밀집 객체 골든 2종으로 객체 선택 면·행 포커스·후보 포커스를 각각 승인 |
| 99 | 새 Scan ID마다 객체 ID가 달라지는 장기 실행에서 행 FocusNode가 누적될 위험 | Preview와 같은 현재 detection 집합 기반 정리 정책으로 사라진 행 노드를 frame 종료 뒤 dispose하고 저장 중에는 모든 행을 Tab 순서와 실행 상태에서 함께 제거 |
| 100 | Top-3 3개와 상품 검색 결과 최대 8개가 각각 독립 Tab 정류장이어서 상품 하나를 확인할 때 같은 선택 그룹을 반복 탐색 | 공통 `_ProductChoiceGroup` 로빙 포커스로 그룹마다 현재 탐색 행 하나만 Tab 순서에 유지. `↑/↓/←/→`는 그룹 안을 순환하고 Top-3의 Shift+Tab은 같은 객체 행, 검색 결과의 Shift+Tab은 검색 입력으로 복귀 |
| 100 | 후보를 훑는 방향키가 곧바로 상품을 확정하면 오입력만으로 판정과 자동 다음 객체 이동이 발생할 수 있음 | 방향키는 포커스 링과 다음 Tab 정류장만 이동하고 `finalProduct`·선택 객체·검색 상태를 보존. Enter·Space 또는 pointer 클릭만 기존 후보/검색 확정 계약을 실행 |
| 100 | 검색어 변경이나 다음 객체 전환 뒤 이전 검색 결과의 로빙 위치·FocusNode가 남으면 첫 결과 진입과 장기 세션 메모리가 불안정 | 객체 ID·검색어 기반 group ID 변경 시 첫 행으로 로빙 위치를 초기화하고 사라진 비첫행 FocusNode는 frame 종료 뒤 정리. 첫 행의 공유 FocusNode는 기존 자동 다음 후보·최종 CTA 인계를 유지 |
| 101 | 상품 검색 결과가 최대 8개여도 `1280×720` 검수 패널에는 4개만 보여 전체 결과 수와 아래에 더 있다는 사실을 알기 어려움 | 검색 입력 아래에 `검색 결과 n개` 또는 `상위 8 / n개` 헤더를 추가하고 검수 inspector에 테마 기반 8px 상시 Scrollbar를 제공해 수량과 스크롤 가능성을 동시에 노출 |
| 101 | 방향키로 화면 밖 다섯 번째 이후 결과에 포커스를 옮겨도 스크롤이 따라오지 않으면 실행 대상이 보이지 않은 채 Enter·Space를 누르게 됨 | 로빙 이동 뒤 `Scrollable.ensureVisible`을 사용해 진행 방향 기준으로 포커스 카드 전체를 inspector 안에 노출. reduced motion에서는 즉시, 일반 환경에서는 motion token으로 이동 |
| 101 | 오프스크린 결과에서 검색창으로 Shift+Tab한 뒤 다시 Tab하면 공간 기반 traversal이 현재 로빙 행을 찾지 못해 포커스가 사라짐 | 검색 입력의 forward Tab을 검색 결과 그룹의 현재 로빙 FocusNode에 명시적으로 연결하고 같은 ensure-visible 정책을 재사용. 검색어·객체가 바뀌면 inspector 스크롤을 상단으로 초기화 |
| 102 | 확정한 상품을 다시 변경하려고 검색을 열면 모든 검색 결과가 미선택 radio로 보여 현재 값과 새 후보를 비교할 기준이 사라짐 | 검색 inspector에 `n번 상품 검색/변경` 문맥과 `현재 상품 이름 · class ID`를 표시하고, 결과에 현재 상품이 포함되면 기존 Brand Soft 면·사방 Brand 외곽선·선택 radio·Semantics `selected`를 그대로 적용 |
| 102 | 단일 선택 그룹을 다시 열었는데 Tab이 현재 선택이 아닌 첫 결과로 이동하면 화면 밖 현재 값을 찾기 위해 방향키를 반복해야 함 | `_ProductChoiceGroup`의 초기 로빙 항목을 `selected` 결과 우선으로 정하고, 선택 결과가 없을 때만 첫 행을 사용. 검색 입력에서 Tab하면 현재 상품 카드 전체를 자동 노출하고 Top-3 재검수에도 같은 radio-group 관례를 적용 |
| 103 | 상품 검색 TextField에 포커스가 있으면 전역 단축키 보호가 `Esc`까지 차단할 수 있고, 검색을 닫은 뒤 사라진 입력에 포커스가 남아 키보드 작업 흐름이 끊김 | 검색 범위가 `Esc`를 직접 처리하고 현재 상품 선택은 그대로 보존한 뒤 `다른 상품 검색/상품 변경` 진입 행동으로 포커스를 복원. 뒤로 아이콘의 Enter·Space도 같은 복귀 계약을 사용하고 pointer 취소는 포커스를 강제로 이동하지 않음 |
| 103 | 이미 확정된 동일 상품을 검색 결과에서 다시 선택하면 실제 상품 변경이 없는데도 `confirmation_method`가 `SEARCH_SELECTED` 또는 `USER_CORRECTED`로 덮여 Activity가 수정으로 집계될 수 있음 | class ID가 기존 `finalProduct`와 같으면 검색을 닫고 다음 검수 위치로 진행하되 기존 `AUTO_APPROVED`·`TOP3_SELECTED`·`SEARCH_SELECTED` 확정 근거와 `user_modified` 의미를 보존. 다른 상품을 선택했을 때만 기존 수정 계약을 적용 |
| 103 | 뒤로 아이콘의 `후보로 돌아가기`가 Top-3가 없는 APPROVED 상품 검색에도 노출되면 돌아갈 후보가 있는 것처럼 안내 | Top-3가 있으면 `후보로 돌아가기`, 없으면 `검색 닫기`로 실제 복귀 화면에 맞춰 접근 가능한 이름과 tooltip을 전환 |

## 금지 사항

- BIXOLON Orange 위에 흰색 일반 텍스트를 사용하지 않습니다.
- 장식 목적으로 Orange 면, 대각선, 상태 배지를 반복하지 않습니다.
- 선택 가능한 객체·상품·활동 행에 왼쪽 띠만으로 선택 상태를 표시하지 않습니다. 하단선은 상단 탐색의 현재 위치 표시에만 사용합니다.
- 같은 화면에 서로 경쟁하는 Primary action을 두 개 이상 노출하지 않습니다.
- raw tensor, logits, 내부 예외, 로컬 경로를 UI에 표시하지 않습니다.
- `ERROR`를 재촬영 안내로 바꾸거나 `RECAPTURE`를 시스템 장애처럼 표현하지 않습니다.
