{
  "version": "0.2.0",
  "work": "artifacts/retraining/n100-0.2.0",
  "baseline": "artifacts/versions/0.1.18/staging",
  "log_manifest": "artifacts/retraining/three-bakery-improvement-0.1.18/log-inputs.jsonl",
  "original_manifest": "artifacts/retraining/three-bakery-revised300/prepared/original_detection.jsonl",
  "targets": {
    "image_count": 132,
    "ground_truth_count": 1096,
    "minimum_correct_approved_count": 1042,
    "maximum_wrong_approved_count": 0,
    "maximum_missed_count": 0,
    "maximum_http_p95_ms": 200,
    "match_iou": 0.5
  },
  "hardware_target": "Intel N100 with integrated Intel UHD GPU",
  "execution": {
    "provider": "cpu",
    "embedder_provider": "openvino_gpu",
    "embedder_fallback_provider": "same",
    "detector_threads": 2,
    "embedder_threads": 4,
    "concurrent_requests": 1,
    "warmup": 10,
    "repetitions": 3,
    "verifier_provider": "cpu",
    "provider_execution_cpu_fallback": true
  },
  "data_policy": "All 132 log images remain excluded from model weight training. Subsequent 0.2.0 development uses these exposed logs for global detector threshold and local recapture policy selection, as well as candidate comparison. This is development tuning, not independent validation; the initial calibration exclusion statement no longer describes this later phase. All GT objects remain in the denominator; final300 is excluded from selection.",
  "proposal_diagnosis": {
    "score_thresholds": [
      0.25,
      0.4,
      0.5
    ],
    "nms_iou_thresholds": [
      0.3,
      0.5
    ],
    "containment_thresholds": [
      0.9,
      null
    ]
  },
  "selected_candidate": "artifacts/retraining/n100-0.2.0/candidates/dfine-dense-local-recapture"
}
